# Implementation Review Checklist

## 仕様・設計との照合

- [x] 実装が要件、外部設計、内部設計、テスト方針、開発標準と整合しているか。
  - 検証結果: 指摘あり
  - 確認根拠: 参照元PDF取得は所有者、削除中、PDF種別、共有データソース配下、`application/pdf` 配信が実装されている。成果物配信は所有者、削除中、保存済み領域、許可MIME配信が実装されている。一方で、成果物配信処理設計の「MIMEタイプと保存済みファイル実体を確認する」「MIMEタイプは保存時メタ情報と配信時実体で矛盾しない」、および「保存済みファイルなしはHTTP 404へ変換し、データ不整合として記録する」に対して実装差分がある。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-00_F006成果物MIMEと拡張子の不一致を配信前に拒否していない.md`
    - `.issue/implement-from-docs/2026-06-23_07-05-01_F006保存済み成果物欠損がデータ不整合として記録されない.md`
- [x] ディレクトリ構成が設計書や開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: UseCaseは `src/backend/application/references/` と `src/backend/application/artifacts/`、Port/DTOは `src/backend/application/ports/`、Storeは `src/backend/infrastructure/filesystem/`、DB Repositoryは `src/backend/infrastructure/database/repositories/`、REST境界は `src/backend/presentation/rest/` に配置され、開発標準の層責務と一致している。
- [x] ファイル構成、ファイル名、配置先が設計書や開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `get_reference_data.py`、`get_artifact.py`、`reference_store.py`、`artifact_store.py`、`delivery.py` は対象責務を表すファイル名で配置され、既存の router registration へ `delivery_router` として登録されている。
- [x] 外部インターフェース、永続化、ファイル、設定、ログ、エラー、状態名などが設計された契約と一致しているか。
  - 検証結果: 指摘あり
  - 確認根拠: REST URLは `/api/references/{reference_id}` と `/api/artifacts/{artifact_id}`、認証依存関係、UUID path parameter、FileResponse、`x-content-type-options: nosniff` は契約と一致している。DB Repositoryは所有者と `deleting` を確認している。一方、成果物配信でMIMEと拡張子の対応を検証せず、保存済みファイル欠損が `trace=False` のためデータ不整合として記録されない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-00_F006成果物MIMEと拡張子の不一致を配信前に拒否していない.md`
    - `.issue/implement-from-docs/2026-06-23_07-05-01_F006保存済み成果物欠損がデータ不整合として記録されない.md`
- [x] 設計書にない状態、入出力項目、設定値、永続化項目、操作導線、利用者向け文言が追加されていないか。
  - 検証結果: 指摘なし
  - 確認根拠: F006実装は既存の `AppError` 分類、既存DB model、既存設定 `data_source.dir` / `generator.saved_artifacts_dir`、既存共通エラー応答を利用しており、新規状態、DB項目、設定項目、画面導線は追加していない。
- [x] 実装が正しく、仕様書側が古い可能性がある場合は、仕様書側の修正方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 今回の差分は仕様書側の古さではなく、成果物配信処理設計の不変条件と例外処理に実装が追従していない問題と判断した。
  - 理由: 仕様書側修正ではなく実装・テスト修正対象のため。
- [x] 対象成果物に存在しない技術要素や実行形態を前提にして指摘していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 指摘はFastAPI REST、SQLAlchemy Repository、ファイルStore、AppError/TraceLogという実在する対象成果物だけに基づく。存在しない外部サービス、ブラウザ実行、総合テストを前提にしていない。

## 責務分割と依存方向

- [x] 層、機能、モジュール、コンポーネントの責務が設計と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: presentationは認証、設定取得、UseCase呼出、FileResponse化に限定され、applicationはRepository/Store調停と分類判断、infrastructureはDBとファイル実体を扱っている。
- [x] 副作用を持つ処理が定義済みの境界へ閉じているか。
  - 検証結果: 指摘なし
  - 確認根拠: DB読込は `SqlAlchemyChatRepository`、ファイル存在/読込は `FileReferenceStore` / `FileArtifactStore`、HTTP応答は `delivery.py` に閉じている。UseCaseはPort越しに副作用境界を呼び出している。
- [x] 業務判断やドメイン判断が、表示層、入出力層、インフラ層、設定読込など不適切な場所へ漏れていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 所有者・削除中判定はRepository、PDF種別と成果物MIME許可はUseCase、パス許可範囲はStore/PathSecurityServiceで扱われ、presentationに業務判断は持ち込まれていない。
- [x] 純粋ロジックが永続化、通信、ファイル、時刻、ID、外部プロセスなどの副作用へ直接依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: F006のUseCaseはPort抽象に依存し、直接SQLAlchemyやPath操作を行っていない。ファイル読込確認はStore実装に隔離されている。
- [x] 上位層が下位層の実装詳細、テスト用実体、内部データ形式へ直接依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: UseCaseはProtocolとDTOに依存し、テストFakeやSQLAlchemy modelへ依存していない。presentationは既存の具象組立パターンとしてRepository/Storeを生成するだけで、DB modelやlocator JSONの内部形状を扱っていない。
- [x] 依存方向、公開範囲、再利用単位が設計や開発標準と矛盾していないか。
  - 検証結果: 指摘なし
  - 確認根拠: applicationからinfrastructureへの直接依存はなく、infrastructureがapplication Port DTOを実装する向きになっている。router登録は既存 `register_routes` に閉じている。
- [x] 不要な依存関係、未使用コード、到達不能コード、暫定的な分岐、デバッグ用処理が残っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象実装にデバッグ出力、暫定コメント、未使用の手動分岐は確認されなかった。生成役報告でもruff check passが示されている。

## 型と構造化データ

- [x] 使用言語、フレームワーク、開発標準に照らして、構造化データが意味のある型、データ構造、スキーマ、列挙値で表現されているか。
  - 検証結果: 指摘なし
  - 確認根拠: Command/Result/Opened DTO は dataclass、PortはProtocol、DB戻り値は `DisplayReferenceData` / `ArtifactData`、エラー分類は `ErrorType` で表現されている。
- [x] 広すぎる型、未検証の動的データ、説明できない型変換、暗黙のデータ形状に依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: `ReferenceModel.locator` は `_reference_data` で `path`、`page_start`、`page_end` の型を検証してからDTO化している。対象ファイル内に `Any`、`cast(...)`、広すぎる `object` による回避は確認されなかった。
- [x] 外部境界、永続化境界、設定境界、表示境界、処理内部のデータ構造が必要に応じて分かれているか。
  - 検証結果: 指摘なし
  - 確認根拠: DB model、application DTO、filesystem DTO、HTTP FileResponseが分離されており、DB modelを直接HTTP応答へ返していない。
- [x] ID、状態、種別、payload、メタデータが文字列や汎用コンテナだけに埋もれず、意味のある表現になっているか。
  - 検証結果: 指摘なし
  - 確認根拠: `reference_id` / `artifact_id` はUUID、チャット状態は `ChatState`、run状態は `RunState`、参照元/成果物メタは専用DTOで表現されている。
- [x] 入力値の検証、正規化、変換が境界で行われ、内部処理が未検証データを前提にしていないか。
  - 検証結果: 指摘あり
  - 確認根拠: UUID path parameter、locator型、PDF拡張子、保存済み成果物の許可拡張子、許可MIMEは検証されている。一方、保存済み成果物の `storage_path` 拡張子と `mime_type` の対応関係は検証されていない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-00_F006成果物MIMEと拡張子の不一致を配信前に拒否していない.md`

## エラー・ログ・セキュリティ

- [x] 利用者向けメッセージと内部調査用情報が分離されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 実装は `AppError` と共通RESTエラー変換を使い、利用者向け応答は共通メッセージ、内部診断は `diagnostic_message` と `trace` に分離している。
- [x] 内部パス、秘密情報、スタックトレース、認証情報、個人情報などが利用者向け出力、外部応答、ログ、証跡へ露出していないか。
  - 検証結果: 指摘なし
  - 確認根拠: REST応答は `FileResponse` または共通エラーpayloadであり、テストでも内部パスやstorage_pathを本文へ含めないことを確認している。診断文は `trace=False` では空化される。
- [x] URL、ポート、ファイルパス、認証情報、環境名、外部サービス名などが不適切にハードコードされていないか。
  - 検証結果: 指摘なし
  - 確認根拠: ルートURLは外部IFの `/api/references/{reference_id}` と `/api/artifacts/{artifact_id}`、ファイルルートは設定値から取得され、認証情報や個人パスのハードコードは確認されなかった。
- [x] エラー分類、追跡情報、状態更新、後続処理の継続または抑止が設計と一致しているか。
  - 検証結果: 指摘あり
  - 確認根拠: 対象なし404、削除中409、許可外403、読込失敗500は実装されている。一方、保存済み成果物実体欠損は設計上「HTTP 404へ変換し、データ不整合として記録する」必要があるが、現在は `trace=False` のため記録されない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-01_F006保存済み成果物欠損がデータ不整合として記録されない.md`
- [x] ファイルパス、URL、コンテンツ種別、マークアップ、コマンド、外部入力などの検証が境界で行われているか。
  - 検証結果: 指摘あり
  - 確認根拠: `PathSecurityService` は絶対パス、親ディレクトリ、URL、UNC、NUL、許可外拡張子を拒否している。成果物MIMEはUseCaseで許可集合を確認するが、コンテンツ種別と保存済みファイル拡張子の対応検証がない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-00_F006成果物MIMEと拡張子の不一致を配信前に拒否していない.md`
- [x] 認証、認可、入力制限、出力制御、監査記録が対象システムの設計と整合しているか。
  - 検証結果: 指摘あり
  - 確認根拠: 認証Cookie、所有者絞り込み、削除中拒否、出力本文非露出は実装・テストされている。一方、保存済み成果物欠損時のデータ不整合記録が不足している。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-01_F006保存済み成果物欠損がデータ不整合として記録されない.md`

## 実行時制御

- [x] 時刻、乱数、ID、並行処理、リトライ、タイムアウト、リソース解放の扱いが仕様、設計、テストと整合しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F006配信は読み取り処理であり、追加の並行制御、リトライ、タイムアウトを導入していない。DB sessionはcontext manager、ファイル読込確認はwith文、FileResponseはパスを渡す構成になっている。

## コメントと説明

- [x] コメントや docstring は、仕様上の意図、契約、前提条件、非自明な制約の説明に限定されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 追加実装のdocstringは参照元PDF取得、成果物取得、保存済み成果物配信境界など役割説明に限定されている。
- [x] 作業都合、環境制約、暫定理由、レビュー対応理由、カバレッジ都合、旧仕様説明、コードの単なる言い換えがコメントや docstring に混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象実装に「暫定」「レビュー対応」「カバレッジのため」などの作業経緯コメントは確認されなかった。
- [x] 業務上の非自明な前提、セキュリティ制約、外部仕様制約、特定テストデータが必要な理由、mock/fake/stub の境界理由、並行実行や時刻・ID・通信の注意点など、保守に必要な説明は残されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 非自明なセキュリティ制約は `PathSecurityService`、UseCase名、Store名、テストdocstringから追跡できる。追加で必要な説明不足は確認されなかった。

## テストとの対応

- [x] 実装に対応する単体テスト、結合テスト、必要な上位テスト仕様があるか。
  - 検証結果: 指摘あり
  - 確認根拠: F006対象の単体・結合テストは正常配信、所有者、削除中、欠損、読込失敗、パストラバーサル、許可外MIME、HTML/CSVを確認している。一方、MIMEと拡張子の不一致、および保存済み成果物欠損のデータ不整合記録を確認するテストがない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-00_F006成果物MIMEと拡張子の不一致を配信前に拒否していない.md`
    - `.issue/implement-from-docs/2026-06-23_07-05-01_F006保存済み成果物欠損がデータ不整合として記録されない.md`
- [x] 事前条件、事後条件、不変条件、異常系、境界値がテスト対象になっているか。
  - 検証結果: 指摘あり
  - 確認根拠: 主要な正常系・異常系はテストされているが、成果物配信処理設計の不変条件「MIMEタイプは保存時メタ情報と配信時実体で矛盾しない」がテスト対象になっていない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-00_F006成果物MIMEと拡張子の不一致を配信前に拒否していない.md`
- [x] 実装変更に対して証跡やテスト方針が古くなっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: evidenceはF006実装後の単体240 passed、結合136 passed、branch coverage passを記録している。テスト方針側が古いとは判断しない。
- [x] テストが実装詳細だけを固定せず、仕様上の振る舞いや契約を検証しているか。
  - 検証結果: 指摘なし
  - 確認根拠: テストはREST status、Content-Type、本文、内部パス非露出、エラーpayload、Store呼出有無を確認しており、private実装の処理順に依存していない。
- [x] レビュー対象に該当しないテスト種別や証跡形式を必須前提にしていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 今回は結合テスト完了検証であり、総合テストやPlaywright証跡は必須条件として扱っていない。

## 修正方針の判断

- [x] 実装が設計から外れている場合は、実装と関連テストを直す方針を書く。
  - 検証結果: 指摘あり
  - 確認根拠: 作成した2 issueに、成果物MIME/拡張子対応検証と保存済み成果物欠損記録について、実装と関連単体・結合テストを直す方針を記録した。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-23_07-05-00_F006成果物MIMEと拡張子の不一致を配信前に拒否していない.md`
    - `.issue/implement-from-docs/2026-06-23_07-05-01_F006保存済み成果物欠損がデータ不整合として記録されない.md`
- [x] 設計が実装済みの正しい振る舞いを表せていない場合は、設計とテスト仕様を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 今回確認した差分は設計側の古さではなく、実装とテストが設計契約を満たしていない問題である。
  - 理由: 設計修正対象ではないため。
- [x] テストや証跡だけが古い場合は、実装を直す前提にせず、テスト仕様、テストコード、証跡の修正方針を書く。
  - 検証結果: 対象外
  - 確認根拠: テストだけでなく実装にも設計差分があるため、テスト・証跡のみの修正では完了しない。
  - 理由: 実装修正も必要なため。
- [x] 判断にユーザ合意が必要な場合は、どの成果物を確定根拠にするかを修正方針に書く。
  - 検証結果: 対象外
  - 確認根拠: 成果物配信処理設計と成果物ファイルIFから期待動作を一意に判断でき、追加のユーザ合意は不要と判断した。
  - 理由: docsから判定できるため。
- [x] 特定のアプリ、言語、通信方式、画面構成、保存方式に依存した判断ではなく、レビュー対象の仕様と設計に基づいて指摘する。
  - 検証結果: 指摘なし
  - 確認根拠: 指摘はF006の内部設計、外部IF、ファイルIF、処理設計に基づいており、未採用の技術や対象外の画面構成を前提にしていない。
