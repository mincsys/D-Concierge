# Test Review Checklist

## テスト方針との整合

- [x] テストコードとテスト仕様が、対応するテスト方針の範囲指定、テスト対象単位、除外範囲に一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `.tmp/.../system-test/総合テスト方針.md` は正式総合テスト方針のコピーであり、F004 の結果は `チャット実行テスト.md`、`キャンセルテスト.md`、`履歴再表示テスト.md` に追記されている。F004 の対象である SSE、キャンセル、起動時回復は API/SSE/DB/trace log 境界で確認され、F005/F006/F007 相当は後続機能待ちまたは対象外に分類されている。
- [x] テスト関連成果物のディレクトリ構成が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 機能別総合テスト成果物は `.tmp/implement-from-docs-v2/features/F004_execution_sse_cancel/system-test/` 配下に、方針、`テスト仕様・結果/`、`evidence/` の構成で保存されている。
- [x] テスト関連成果物のファイル構成、ファイル名、配置先が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `総合テスト方針.md`、6 本の `テスト仕様・結果/*.md`、`evidence/F004-execution-sse-cancel-api-db.txt`、`evidence/F004-system-test-summary.txt` が存在し、正式総合テストのコピー先として指定された構成と一致している。
- [x] 単体、結合、総合テストの役割が混ざっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: `.tmp` 側総合テスト仕様には機能別総合テストとしての分類・証跡が記録され、単体/結合コマンドは F004 の backend API/SSE/DB/trace log 確認証跡として `F004-execution-sse-cancel-api-db.txt` に分離されている。
- [x] テスト方針で求める観点、カバレッジ、証跡、実行環境、完了条件が満たされているか。
  - 検証結果: 指摘なし
  - 確認根拠: summary では `classification_fail=0`、`classification_environment_wait=0`、`unfinished_items=なし`、承認付き結合実行は `approved_and_passed`。Playwright 未実行理由と正式総合テストへの持ち越し候補も記録されている。
- [x] テスト方針が実装や設計の現状に追従していない場合は、テスト方針側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: `.tmp` 側の分類と evidence は F004 実装範囲と整合し、テスト方針側の修正を要する不整合は確認していない。
  - 理由: テスト方針修正が必要な指摘がないため。

## 単体テスト

- [x] 公開関数、公開メソッド、コンポーネント、純粋ロジックの責務を単位としているか。
  - 検証結果: 指摘なし
  - 確認根拠: evidence は F004 execution unit tests として `test_cancel_and_recovery_use_cases.py` と `test_sse_event_broker.py` を指定し、キャンセル・回復 use case と broker の責務単位で `20 passed` を記録している。
- [x] 外部副作用を Fake、Stub、Mock、fixture で差し替えているか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体側は F004 execution use case と event broker の確認であり、DB 接続や実 Codex 実行ではなく、F004 ロジック境界の確認証跡として記録されている。
- [x] 事前条件、事後条件、不変条件、正常系、異常系、境界値、分岐が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: evidence に `broker_confirmed`、`startup_recovery_confirmed`、`cancel_confirmed` があり、publish/subscribe、terminal suppression、repeated unsubscribe、accepted/running/validating/cancel_requested recovery、cancel 状態遷移を確認済みと記録されている。
- [x] テストが内部実装詳細に密結合しすぎていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 機能別総合テストの証跡としては UseCase/broker の外部観測可能な状態遷移とイベント配信結果を確認しており、private 実装詳細を総合テスト結果へ持ち込んでいない。
- [x] テストが単に例外が出ないことではなく、仕様上意味のある結果、状態変化、副作用、出力を検証しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `db_confirmed`、`trace_log_confirmed`、`api_sse_confirmed` に state 更新、SSE payload、trace log 保存が明記されている。

## 結合テスト

- [x] 外部インターフェース、処理設計、永続化、ファイル、通信境界、表示または操作連携を設計単位で確認しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `integration_command` は `test_execution_sse_cancel_api.py` を実行し、SSE 現在状態、保存済み message/answer/error/canceled、broker live delivery、cancel、DB 状態、起動時回復、trace log を確認したと evidence に記録されている。
- [x] 実外部サービス、実行環境、実行時間や環境差分が大きい対象を不必要に使っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: F004 単独の確認では実 Codex コンテナや画面操作を使わず、F004 の責務である API/SSE/DB/trace log 境界を結合テストで確認し、実 Codex 実行は F005 以降に分類している。
- [x] Mock、Fake、Stub が過剰で、実際の契約違反や結合不備を隠していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 結合 evidence は HTTP/SSE API、DB 状態更新、trace log を確認しており、F004 で不要な Codex 実行本体だけを後続機能待ちにしている。
- [x] トランザクション、状態更新、競合、ロールバック、エラー変換、ログ依頼が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: `cancel_confirmed` に unauthorized、other-user、terminal、deleting、DB state updates があり、`startup_recovery_confirmed` と `trace_log_confirmed` に回復成功/失敗ログが記録されている。
- [x] テストデータが独立し、実行順序に依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: evidence は対象テストファイル単位で `17 passed` を記録しており、summary でも未完了事項なし。順序依存や未分類の失敗は報告されていない。
- [x] 時刻、乱数、実行順序、外部通信、ファイルシステム、DB 状態により不安定になるテストになっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: PostgreSQL 接続が必要な結合確認は承認付きで `approved_and_passed` と記録され、外部 Codex やブラウザ状態に依存する項目は後続機能待ちまたは正式総合テストへ持ち越されている。
- [x] テストデータが実装ロジックのコピーや都合のよい値だけになっておらず、境界値、異常値、業務上意味のある値を含んでいるか。
  - 検証結果: 指摘なし
  - 確認根拠: accepted/running/validating/cancel_requested、terminal、deleting、other-user、unauthorized、`already_exited` / `not_registered`、startup recovery failure など F004 の業務境界が evidence に記録されている。

## 総合テスト

- [x] 利用者が利用者インターフェースまたは実運用に近い経路で業務を完了できることを確認しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F004 機能別では backend API/SSE/DB/trace log 境界を実運用に近い経路として確認し、画面上の SSE 状態表示、キャンセル連打 UI、実 Codex コンテナ連携は正式総合テストまたは後続機能へ持ち越すと summary と state に明記されている。
- [x] 自動確認、手動確認、通信確認、永続化確認、ファイル確認の役割がテスト方針と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: summary は `manual_check=API/DB/SSE/trace log確認`、`playwright_cli=not_run`、`playwright_reason=...` を記録し、通信確認・永続化確認・trace log 確認の役割を分けている。
- [x] 未実施、一部確認、保留、不合格、再テストの記録がテスト仕様・結果に残っているか。
  - 検証結果: 指摘なし
  - 確認根拠: `.tmp` 側のチャット実行、キャンセル、履歴再表示の各 `F004 機能別実施結果` に、合格、部分確認、後続機能待ち、対象外が対象ケース・証跡・備考付きで記録されている。不合格と環境・承認待ちは 0 件。
- [x] 実行証跡とテストケースが追跡できるか。
  - 検証結果: 指摘なし
  - 確認根拠: `F004-system-test-summary.txt` は pass/partial/followup/out_of_scope のケース ID を列挙し、各 `テスト仕様・結果/*.md` の F004 実施結果行も `evidence/F004-execution-sse-cancel-api-db.txt` または `evidence/F004-system-test-summary.txt` を参照している。

## TDD と実装順序

- [x] 実装成果物レビューでは、TDD が要求される範囲で Red、Green、Refactor の証跡または説明があるか。
  - 検証結果: 対象外
  - 確認根拠: 今回は機能別総合テストレビューであり、TDD の Red/Green 判定はテストコードレビューおよび結合・品質レビューで確認済み。
  - 理由: 現フェーズの直接判定対象ではないため。
- [x] Red を事後的に作れない場合は、レビュー指摘として記録し、今後の修正方針を示す。
  - 検証結果: 対象外
  - 確認根拠: F004 の Red 確認は state のテストコード作成結果と結合レビュー履歴に記録済みで、機能別総合テストレビューでは追加 Red 作成を扱わない。
  - 理由: Red 不能の判断を行うフェーズではないため。
- [x] テスト追加が不要な場合は、除外理由が方針と整合しているか。
  - 検証結果: 対象外
  - 確認根拠: 今回は機能別総合テスト結果のレビューであり、テスト追加不要判断は発生していない。
  - 理由: テスト追加要否の指摘がないため。

## 不要なコメントと作業経緯

- [x] テスト名、docstring、コメント、テスト仕様、証跡に、確認対象の理解や再実行に不要な作業経緯、内部事情、言い訳、暫定理由が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: `.tmp` 側の F004 実施結果と evidence は分類理由、実施日、実施者、証跡、持ち越し理由を記録しており、レビュー対応経緯や言い訳の混入は確認していない。
- [x] コメントや docstring は、仕様上の意図、契約、前提条件、非自明な制約の説明に限定されているか。
  - 検証結果: 指摘なし
  - 確認根拠: `.tmp` 側総合テスト仕様の記述は、ケースの観点、前提条件、操作手順、期待結果、F004 での分類理由に限定されている。
- [x] テスト失敗理由、作成時の都合、環境制約、ツール制約、レビュー指摘への反応など、成果物ではなく作業報告や issue に書くべき内容が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: Playwright 未実行理由は F004 の責務範囲と後続機能待ちの分類理由として summary に記録されており、環境制約への逃避ではない。
- [x] `既存実装があるので失敗しない`、`一旦この実装にしている`、`あとで修正する`、`指摘されたため追加`、`カバレッジのため追加`、`環境の都合で確認できない`、`旧仕様ではこうだった`、コードの単なる日本語言い換えなどが残っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: `チャット実行テスト.md`、`キャンセルテスト.md`、`履歴再表示テスト.md` の F004 実施結果と evidence で該当表現は確認していない。
- [x] 業務ルール上の非自明な前提、セキュリティ上の制約、外部仕様に由来する制約、特定テストデータが必要な理由、mock/fake/stub の境界理由、並行実行、時刻、ID、ファイル、通信の注意点など、確認や保守に必要な説明は残されているか。
  - 検証結果: 指摘なし
  - 確認根拠: F004 の持ち越し理由として、画面上の SSE 状態表示、SSE 接続失敗/途中切断、実 Codex コンテナ、回答生成/検証、参照元、成果物、欠損表示が明示されている。

## 修正方針の判断

- [x] テスト不足ならテストコード、テスト仕様、証跡のどれを補うかを書く。
  - 検証結果: 対象外
  - 確認根拠: F004 機能別総合テストとしてのテスト不足は確認されず、新規 issue は作成していない。
  - 理由: 修正方針を記録すべき指摘がないため。
- [x] テストが仕様に従っていて実装が違う場合は、実装側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: `.tmp` 側結果、evidence、state の範囲で仕様と実装結果の不整合は確認されていない。
  - 理由: 実装修正方針が必要な指摘がないため。
- [x] テスト方針が過剰または古い場合は、テスト方針と関連成果物を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 総合テスト方針の修正が必要な不整合は確認されていない。
  - 理由: テスト方針修正方針が必要な指摘がないため。
