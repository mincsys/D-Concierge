# Test Review Checklist

レビュー対象: F004 実行状態・SSE・キャンセル・起動時実行回復 テストコードレビュー round-1
レビュー日: 2026-06-22

## テスト方針との整合

- [x] テストコードとテスト仕様が、対応するテスト方針の範囲指定、テスト対象単位、除外範囲に一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告では単体テストを `src/backend/tests/unit/application/execution/` と Port 契約テスト、結合テストを `src/backend/tests/integration/test_execution_sse_cancel_api.py` に追加している。対象は run 状態遷移、SSE 購読、キャンセル、起動時実行回復であり、単体/結合テスト方針の backend 対象範囲に一致する。
- [x] テスト関連成果物のディレクトリ構成が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F004 の application 単体は `unit/application/execution/`、Port 契約は `unit/application/ports/`、API/DB 結合は `integration/`、補助は `tests/support/execution.py` に配置されているとの生成役報告で、既存 backend テスト構成と整合する。
- [x] テスト関連成果物のファイル構成、ファイル名、配置先が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `test_cancel_and_recovery_use_cases.py`、`test_sse_event_broker.py`、`test_execution_sse_cancel_api.py` は F004 の処理設計であるキャンセル、SSE、起動時回復に対応するファイル名になっている。
- [x] 単体、結合、総合テストの役割が混ざっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告では単体は use case / broker / Port 契約、結合は SSE/cancel API と起動時回復の REST/DB 境界を確認している。総合テスト、Playwright、coverage 証跡作成はこのフェーズに含めていない。
- [x] テスト方針で求める観点、カバレッジ、証跡、実行環境、完了条件が満たされているか。
  - 検証結果: 指摘なし
  - 確認根拠: テストコードレビュー段階として、生成役報告には `ruff check src/backend/tests` pass、unit `13 failed, 122 passed`、integration `6 failed, 66 passed` の Red 結果と失敗理由が記録されている。coverage は本実装後の結合・品質レビュー対象であり、現段階では未要求。
- [x] テスト方針が実装や設計の現状に追従していない場合は、テスト方針側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 依頼情報と生成役報告上、F004 テスト方針や関連設計側の旧化・過剰要求は確認されていない。
  - 理由: テスト方針修正が不要なため。

## 単体テスト

- [x] 公開関数、公開メソッド、コンポーネント、純粋ロジックの責務を単位としているか。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告ではキャンセル・起動時回復 use case、SSE event broker、Port 契約を単体テスト対象にしており、F004 application/runtime 境界の公開責務単位に対応している。
- [x] 外部副作用を Fake、Stub、Mock、fixture で差し替えているか。
  - 検証結果: 指摘なし
  - 確認根拠: 補助ファイル `tests/support/execution.py` が追加され、F004 用 fake/stub/固定値を使う構成と報告されている。単体テスト Red も未定義 application/Port 境界に起因しており、実 DB や実外部プロセスを単体で使っている兆候はない。
- [x] 事前条件、事後条件、不変条件、正常系、異常系、境界値、分岐が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 依頼観点と生成役報告では、キャンセル受付、状態遷移、重複/終端済み/削除中/権限境界、起動時回復の `accepted` / `running` / `validating` / `cancel_requested` 整合、dispatcher 多重登録防止を確認対象としている。
- [x] テストが内部実装詳細に密結合しすぎていないか。
  - 検証結果: 指摘なし
  - 確認根拠: Red 失敗理由が `backend.application.execution` 未作成、`BackgroundExecutorPort` / `ChatRunExecutorPort` 未定義、F004 用 `ChatRepositoryPort` メソッド未定義であることから、テストは公開 application/Port 境界を要求しており、既存 private 実装への密結合ではない。
- [x] テストが単に例外が出ないことではなく、仕様上意味のある結果、状態変化、副作用、出力を検証しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 依頼観点では SSE wire payload、購読解除、終端イベント、保存済み中間メッセージ、現在状態通知、キャンセル状態遷移、起動時回復の状態整合が確認対象であり、単なる no exception テストではない。

## 結合テスト

- [x] 外部インターフェース、処理設計、永続化、ファイル、通信境界、表示または操作連携を設計単位で確認しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `test_execution_sse_cancel_api.py` は SSE/cancel API と起動時回復を結合テスト対象として追加され、外部 IF の SSE 購読、キャンセル要求、DB 状態整合に対応している。
- [x] 実外部サービス、実行環境、実行時間や環境差分が大きい対象を不必要に使っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: Red 結果は F004 API 未登録と起動時回復未実装に起因し、実 Codex 実行、回答検証、成果物配信、物理削除など後続機能を起動した報告はない。
- [x] Mock、Fake、Stub が過剰で、実際の契約違反や結合不備を隠していないか。
  - 検証結果: 指摘なし
  - 確認根拠: integration Red は SSE/cancel API 未登録の 404 と起動時回復未実装による DB 状態未整合を検出しており、過剰な stub で結合不備を隠していない。
- [x] トランザクション、状態更新、競合、ロールバック、エラー変換、ログ依頼が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 依頼観点ではキャンセル受付、状態遷移、重複/終端済み/削除中/権限境界、起動時回復の状態整合を確認対象としている。これは F004 のトランザクション・競合・エラー変換契約に対応する。
- [x] テストデータが独立し、実行順序に依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: F004 補助ファイル `tests/support/execution.py` が追加され、既存 F001-F003 のテスト支援方針と同様に固定 ID/状態データを使う構成と解釈できる。生成役報告上、収集エラーや既存機能破壊はない。
- [x] 時刻、乱数、実行順序、外部通信、ファイルシステム、DB 状態により不安定になるテストになっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告の Red は安定した未実装起因の失敗で、DB 接続エラーや環境差分による失敗は報告されていない。F004 は実 Codex や実成果物ファイルを対象にしていない。
- [x] テストデータが実装ロジックのコピーや都合のよい値だけになっておらず、境界値、異常値、業務上意味のある値を含んでいるか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象状態として `accepted`、`running`、`validating`、`cancel_requested`、終端済み、削除中、他ユーザ、未ログイン、not found が観点に含まれ、F004 の業務境界をカバーしている。

## 総合テスト

- [x] 利用者が利用者インターフェースまたは実運用に近い経路で業務を完了できることを確認しているか。
  - 検証結果: 対象外
  - 確認根拠: 現フェーズは F004 テストコードレビューであり、機能別総合テストは後続フェーズ。
  - 理由: 総合テストレビュー対象ではないため。
- [x] 自動確認、手動確認、通信確認、永続化確認、ファイル確認の役割がテスト方針と一致しているか。
  - 検証結果: 対象外
  - 確認根拠: 今回は単体/結合テストコードのレビューであり、総合テスト仕様・結果や手動証跡は対象外。
  - 理由: 総合テスト成果物が対象外のため。
- [x] 未実施、一部確認、保留、不合格、再テストの記録がテスト仕様・結果に残っているか。
  - 検証結果: 対象外
  - 確認根拠: 機能別総合テスト実施前であり、総合テスト結果分類はまだ作成対象ではない。
  - 理由: 後続フェーズで確認するため。
- [x] 実行証跡とテストケースが追跡できるか。
  - 検証結果: 対象外
  - 確認根拠: 現段階の証跡は生成役報告の Red 結果であり、総合テスト証跡は対象外。
  - 理由: 総合テスト証跡レビューではないため。

## TDD と実装順序

- [x] 実装成果物レビューでは、TDD が要求される範囲で Red、Green、Refactor の証跡または説明があるか。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告にはテスト先行作成後の ruff pass、unit `13 failed, 122 passed`、integration `6 failed, 66 passed` が記録され、失敗理由も F004 本実装未作成に起因すると説明されている。
- [x] Red を事後的に作れない場合は、レビュー指摘として記録し、今後の修正方針を示す。
  - 検証結果: 対象外
  - 確認根拠: F004 テストコードは本実装前に Red が成立しているとの生成役報告がある。
  - 理由: Red 欠落指摘が不要なため。
- [x] テスト追加が不要な場合は、除外理由が方針と整合しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F004 は本実装前のテスト追加フェーズであり、対象外は総合テストや F005 以降の後続機能に限定されている。

## 不要なコメントと作業経緯

- [x] テスト名、docstring、コメント、テスト仕様、証跡に、確認対象の理解や再実行に不要な作業経緯、内部事情、言い訳、暫定理由が混入していないか。
  - 検証結果: 判断不能
  - 確認根拠: ユーザー指示によりコマンド実行が禁止され、今回の判定は提示された生成役報告と差分情報に基づく。テスト本文の docstring/comment を直接参照できていないため、不要コメント混入の有無は確定できない。
  - 不足根拠: テストファイル本文の提示または読取可能な確認手段がないため。
- [x] コメントや docstring は、仕様上の意図、契約、前提条件、非自明な制約の説明に限定されているか。
  - 検証結果: 判断不能
  - 確認根拠: 生成役報告では対象テストファイルの追加・更新と Red 結果は示されているが、docstring 本文までは提示されていない。
  - 不足根拠: docstring 本文を確認できる入力がないため。
- [x] テスト失敗理由、作成時の都合、環境制約、ツール制約、レビュー指摘への反応など、成果物ではなく作業報告や issue に書くべき内容が混入していないか。
  - 検証結果: 判断不能
  - 確認根拠: Red 失敗理由は生成役報告に記録されているが、同内容がテストコード本文へ混入していないかは本文未確認のため判定できない。
  - 不足根拠: テストコード本文の確認ができないため。
- [x] `既存実装があるので失敗しない`、`一旦この実装にしている`、`あとで修正する`、`指摘されたため追加`、`カバレッジのため追加`、`環境の都合で確認できない`、`旧仕様ではこうだった`、コードの単なる日本語言い換えなどが残っていないか。
  - 検証結果: 判断不能
  - 確認根拠: 生成役報告には該当する作業都合表現はないが、テストコード本文を直接参照していない。
  - 不足根拠: テストコード本文のコメント・docstring 確認ができないため。
- [x] 業務ルール上の非自明な前提、セキュリティ上の制約、外部仕様に由来する制約、特定テストデータが必要な理由、mock/fake/stub の境界理由、並行実行、時刻、ID、ファイル、通信の注意点など、確認や保守に必要な説明は残されているか。
  - 検証結果: 判断不能
  - 確認根拠: 依頼観点上は SSE、キャンセル、起動時回復、dispatcher 多重登録防止の非自明な契約を確認対象としているが、テスト本文の説明記載までは確認できない。
  - 不足根拠: テストコード本文が未確認のため。

## 修正方針の判断

- [x] テスト不足ならテストコード、テスト仕様、証跡のどれを補うかを書く。
  - 検証結果: 判断不能
  - 確認根拠: 生成役報告ベースでは F004 の主要観点は広く含まれているが、テスト本文未確認により、期待値・assert 粒度・docstring の品質までは断定できない。
  - 不足根拠: テストコード本文を直接確認できていないため。
- [x] テストが仕様に従っていて実装が違う場合は、実装側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 現段階は本実装未作成であり、Red は F004 未実装起因と報告されている。
  - 理由: 実装側の合否判定フェーズではないため。
- [x] テスト方針が過剰または古い場合は、テスト方針と関連成果物を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 生成役報告と依頼情報上、テスト方針の過剰・旧化は確認されていない。
  - 理由: テスト方針修正指摘が不要なため。
