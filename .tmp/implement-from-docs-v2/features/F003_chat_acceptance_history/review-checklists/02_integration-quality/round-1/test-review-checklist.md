# Test Review Checklist

レビュー対象: F003 アプリ設定・チャット受付・履歴再表示 結合テスト完了検証・実装コード品質レビュー round-1
レビュー日: 2026-06-21

## テスト方針との整合

- [x] テストコードとテスト仕様が、対応するテスト方針の範囲指定、テスト対象単位、除外範囲に一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: backend 単体は use case/Port 単位、backend 結合は REST API と PostgreSQL DB 状態確認であり、実 Codex、実ブラウザ、総合テストを含めていない。
- [x] テスト関連成果物のディレクトリ構成が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストは `src/backend/tests/unit/`、結合テストは `src/backend/tests/integration/`、補助は `src/backend/tests/support/` に置かれている。
- [x] テスト関連成果物のファイル構成、ファイル名、配置先が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `test_chat_acceptance_use_cases.py`、`test_chat_history_use_cases.py`、`test_chat_port_contracts.py`、`test_chat_acceptance_history_api.py` が F003 の処理単位と結合対象に対応している。
- [x] 単体、結合、総合テストの役割が混ざっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体は fake repository/dispatcher を使い、結合は FastAPI と PostgreSQL を通す。Playwright や正式総合テスト結果は要求していない。
- [x] テスト方針で求める観点、カバレッジ、証跡、実行環境、完了条件が満たされているか。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告で ruff/format/mypy/diff check、単体 122 passed、結合 66 passed。coverage は単体 95.89%、結合 80.77% で方針値を満たす。
- [x] テスト方針が実装や設計の現状に追従していない場合は、テスト方針側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: テスト方針と F003 の実装・証跡に矛盾は見当たらない。
  - 理由: テスト方針側修正が不要なため。

## 単体テスト

- [x] 公開関数、公開メソッド、コンポーネント、純粋ロジックの責務を単位としているか。
  - 検証結果: 指摘なし
  - 確認根拠: `StartChatUseCase.execute`、`AppendChatRunUseCase.execute`、`ListChatHistoriesUseCase.execute`、`GetChatDetailUseCase.execute`、Port/DTO 公開契約を対象にしている。
- [x] 外部副作用を Fake、Stub、Mock、fixture で差し替えているか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストは `FakeChatRepository`、`FakeRunExecutionDispatcher`、`FakeTransactionManager`、固定 clock/UUID を使い、実 DB/HTTP/外部実行を使っていない。
- [x] 事前条件、事後条件、不変条件、正常系、異常系、境界値、分岐が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 空入力、対象なし、削除中、未完了 run、dispatcher 失敗、履歴一覧、履歴詳細、未生成回答境界を確認している。
- [x] テストが内部実装詳細に密結合しすぎていないか。
  - 検証結果: 指摘なし
  - 確認根拠: テストは use case の戻り値、Port への依頼、例外分類を確認し、private helper や SQLAlchemy 内部を固定していない。
- [x] テストが単に例外が出ないことではなく、仕様上意味のある結果、状態変化、副作用、出力を検証しているか。
  - 検証結果: 指摘なし
  - 確認根拠: accepted 応答、SSE URL、保存依頼、run error 化、field_errors、履歴順、回答/参照元変換を具体値で検証している。

## 結合テスト

- [x] 外部インターフェース、処理設計、永続化、ファイル、通信境界、表示または操作連携を設計単位で確認しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F003 の `GET /api/app-config`、`POST /api/chats/start`、`POST /api/chats/{chat_id}/runs`、`GET /api/chat-histories`、`GET /api/chats/{chat_id}` を REST/DB 境界で確認している。
- [x] 実外部サービス、実行環境、実行時間や環境差分が大きい対象を不必要に使っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 結合テストは PostgreSQL テスト DB と FastAPI ASGITransport を使い、実 Codex、実ブラウザ、外部ネットワークを使っていない。
- [x] Mock、Fake、Stub が過剰で、実際の契約違反や結合不備を隠していないか。
  - 検証結果: 指摘なし
  - 確認根拠: dispatcher 失敗は monkeypatch で境界だけ差し替えるが、REST、認証、DB、Repository、エラー変換は実装を通して検証している。
- [x] トランザクション、状態更新、競合、ロールバック、エラー変換、ログ依頼が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 空入力/未ログイン/対象なし/削除中/未完了 run で DB 非更新を確認し、dispatcher 失敗時に run が `error` になることを確認している。
- [x] テストデータが独立し、実行順序に依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 各結合テストは `prepare_foundation_database` と seed helper で DB を既知状態へ作り、固定 ID/時刻をケース内で使っている。
- [x] 時刻、乱数、実行順序、外部通信、ファイルシステム、DB 状態により不安定になるテストになっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: ID/時刻は固定値や DB seed によって観測され、外部通信は ASGITransport 内に閉じる。実行順依存の共有状態は見当たらない。
- [x] テストデータが実装ロジックのコピーや都合のよい値だけになっておらず、境界値、異常値、業務上意味のある値を含んでいるか。
  - 検証結果: 指摘なし
  - 確認根拠: 空白入力、別ユーザ、削除中、未完了 run、回答未生成 run、回答/参照元あり run、dispatcher 失敗など業務上の境界を含む。

## 総合テスト

- [x] 利用者が利用者インターフェースまたは実運用に近い経路で業務を完了できることを確認しているか。
  - 検証結果: 対象外
  - 確認根拠: 現フェーズは結合テスト完了検証・実装品質レビューであり、機能別総合テストは後続フェーズ。
  - 理由: 総合テストレビュー対象ではないため。
- [x] 自動確認、手動確認、通信確認、永続化確認、ファイル確認の役割がテスト方針と一致しているか。
  - 検証結果: 対象外
  - 確認根拠: 総合テスト仕様・結果や手動/Playwright 証跡は今回のレビュー対象ではない。
  - 理由: 総合テスト成果物が対象外のため。
- [x] 未実施、一部確認、保留、不合格、再テストの記録がテスト仕様・結果に残っているか。
  - 検証結果: 対象外
  - 確認根拠: 機能別総合テスト実施前のため、総合テスト仕様・結果の分類確認は行わない。
  - 理由: 後続フェーズで確認するため。
- [x] 実行証跡とテストケースが追跡できるか。
  - 検証結果: 対象外
  - 確認根拠: 総合テスト証跡ではなく、今回は単体/結合 evidence を確認対象にした。
  - 理由: 総合テスト証跡が対象外のため。

## TDD と実装順序

- [x] 実装成果物レビューでは、TDD が要求される範囲で Red、Green、Refactor の証跡または説明があるか。
  - 検証結果: 指摘なし
  - 確認根拠: state にテストコード先行作成の Red、round-2 合格、生成役実装後の静的/単体/結合/coverage pass が記録されている。
- [x] Red を事後的に作れない場合は、レビュー指摘として記録し、今後の修正方針を示す。
  - 検証結果: 対象外
  - 確認根拠: F003 は本実装前 Red が記録されており、事後 Red 欠落の状態ではない。
  - 理由: Red 欠落指摘が不要なため。
- [x] テスト追加が不要な場合は、除外理由が方針と整合しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F003 では実装に対応する単体/結合テストが追加済みで、不要扱いによる未確認箇所は確認されていない。

## 不要なコメントと作業経緯

- [x] テスト名、docstring、コメント、テスト仕様、証跡に、確認対象の理解や再実行に不要な作業経緯、内部事情、言い訳、暫定理由が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: F003 テスト docstring は観点/確認を説明し、レビュー対応理由や実装都合の説明は混入していない。
- [x] コメントや docstring は、仕様上の意図、契約、前提条件、非自明な制約の説明に限定されているか。
  - 検証結果: 指摘なし
  - 確認根拠: docstring は各テストの設計観点と期待結果を記載している。
- [x] テスト失敗理由、作成時の都合、環境制約、ツール制約、レビュー指摘への反応など、成果物ではなく作業報告や issue に書くべき内容が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: Red 理由や承認付き実行事情は state/evidence/報告にあり、テストコード本文には混入していない。
- [x] `既存実装があるので失敗しない`、`一旦この実装にしている`、`あとで修正する`、`指摘されたため追加`、`カバレッジのため追加`、`環境の都合で確認できない`、`旧仕様ではこうだった`、コードの単なる日本語言い換えなどが残っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テスト・補助コードで該当語句や趣旨のコメントは見当たらない。
- [x] 業務ルール上の非自明な前提、セキュリティ上の制約、外部仕様に由来する制約、特定テストデータが必要な理由、mock/fake/stub の境界理由、並行実行、時刻、ID、ファイル、通信の注意点など、確認や保守に必要な説明は残されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 未ログイン、ユーザ分離、deleting 除外、dispatcher 失敗、DB 非更新などの非自明な契約はテスト名と docstring から追える。

## 修正方針の判断

- [x] テスト不足ならテストコード、テスト仕様、証跡のどれを補うかを書く。
  - 検証結果: 対象外
  - 確認根拠: F003 の現在範囲に新規テスト不足は確認されていない。
  - 理由: 新規 issue がないため。
- [x] テストが仕様に従っていて実装が違う場合は、実装側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: テストと実装の不一致は確認されていない。
  - 理由: 実装修正指摘がないため。
- [x] テスト方針が過剰または古い場合は、テスト方針と関連成果物を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: テスト方針の過剰・旧化は確認されていない。
  - 理由: テスト方針修正指摘がないため。
