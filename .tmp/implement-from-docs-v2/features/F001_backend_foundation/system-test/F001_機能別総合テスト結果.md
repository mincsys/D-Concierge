# F001 機能別総合テスト結果

## 1. 対象

- 機能ID: F001
- 機能名: backend 基盤・設定・DB・共通境界
- 実施日: 2026-06-21
- 実施者: Codex 生成役
- 公式総合テストコピー元: `docs/04_テスト/04_総合テスト/`
- 機能別総合テスト保存先: `.tmp/implement-from-docs-v2/features/F001_backend_foundation/system-test/`

## 2. 判定方針

公式総合テスト仕様は、認証、チャット実行、キャンセル、履歴再表示、チャット削除、アカウント管理の業務シナリオ単位で定義されている。F001はこれらの前提となるbackend基盤であり、画面操作、認証業務、チャット業務、削除業務はF002以降の実装が必要である。

そのため、F001機能別総合テストでは公式ケースをそのまま合格判定せず、F001単独で確認できるbackend境界をsmokeとして実行し、公式ケース上は `部分確認` または `後続機能待ち` に分類した。

## 3. 実施したF001 smoke

| 確認ID | 確認内容 | 実行方法 | 結果 | 証跡 |
| --- | --- | --- | --- | --- |
| F001-SMOKE-001 | app creation / config load が成功すること | `test_app_config_api.py` のASGI HTTP確認 | 合格 | `evidence/F001-backend-smoke.txt` |
| F001-SMOKE-002 | `/api/app-config` が保護APIとして共通エラー形式と `x-trace-id` を返すこと | `test_get_app_config_rejects_missing_or_invalid_session_cookie` | 合格 | `evidence/F001-backend-smoke.txt` |
| F001-SMOKE-003 | REST共通エラー、trace_id header、trace log保存、設定読込失敗時trace logが機能すること | `test_trace_error_boundary.py` と `test_rest_error_boundary.py` | 合格 | `evidence/F001-backend-smoke.txt` |
| F001-SMOKE-004 | DB migration、主要制約、索引、Repository境界を支えるDB制約が作成されること | `test_database_migrations.py` | 合格 | `evidence/F001-backend-smoke.txt` |

## 4. 実行コマンド

```text
env UV_CACHE_DIR=/tmp/uv-cache uv run pytest src/backend/tests/integration/test_app_config_api.py src/backend/tests/integration/test_trace_error_boundary.py src/backend/tests/integration/test_rest_error_boundary.py src/backend/tests/integration/test_database_migrations.py -q
```

結果:

```text
28 passed in 1.24s
```

## 5. 分類別件数

| 分類 | 件数 | 内容 |
| --- | ---: | --- |
| 合格 | 0 | 公式総合テストケースを正式手順どおり完了できたケースはF001単独ではなし |
| 不合格 | 0 | 実施したF001 smokeに不合格なし |
| 部分確認 | 5 | ST-AUTH-003、ST-CHAT-001、ST-CHAT-002、ST-HISTORY-001、ST-DELETE-013 |
| 後続機能待ち | 96 | F002以降の認証、チャット、履歴、削除、アカウント管理が必要なケース |
| 環境・承認待ち | 0 | なし |
| 対象外 | 0 | なし |

## 6. Playwright実行結果

未実行。F001単独ではログイン画面、開始画面、チャット画面、履歴、削除、設定ダイアログの業務画面が完結しないため、Chrome/Playwrightによる画面確認は正式総合テストへ持ち越す。

## 7. 手動確認結果

- 公式総合テスト仕様コピーは `.tmp/implement-from-docs-v2/features/F001_backend_foundation/system-test/` 配下に存在することを確認した。
- F001単独で実施可能なbackend境界確認は、既存の結合テストを用いたsmokeとして実行し、28件すべて合格した。
- 画面操作、Cookie、DB業務状態、Codex実行コンテナ、成果物表示の手動確認は、該当業務機能がF001時点で未実装のため後続機能実装後へ持ち越す。
- `docs/04_テスト/04_総合テスト/` 正本は変更しない。

## 8. 正式総合テストへの持ち越し

- 認証画面、登録、ログイン、ログアウト、セッション維持、セッション期限切れはF002で実装後に正式総合テストで確認する。
- チャット開始、継続指示、SSE、Codex実行、回答表示、参照元、Codex成果物はF003以降で実装後に正式総合テストで確認する。
- キャンセルはF004実装後に正式総合テストで確認する。
- 履歴一覧、履歴詳細、更新日時降順表示、保存済み回答再表示はF003以降で実装後に正式総合テストで確認する。
- チャット削除、削除中競合、物理削除、トレースログ保持を含む削除後確認はF007実装後に正式総合テストで確認する。
- アカウント管理とアカウント削除はF002およびF007実装後に正式総合テストで確認する。

## 9. 未解決事項

F001単独のbackend smokeとしての未解決事項はなし。公式総合テストケースの正式な合格判定は、F002以降の業務機能実装後に実施する。
