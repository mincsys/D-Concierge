# Test Review Checklist round-3

## テスト方針との整合

- [x] テストコードとテスト仕様が、対応するテスト方針の範囲指定、テスト対象単位、除外範囲に一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F001 の単体テストは `src/backend/tests/unit/`、結合テストは `src/backend/tests/integration/`、補助コードは `src/backend/tests/support/` に配置されている。round-2 指摘後、`test_app_config_api.py` は認証 Cookie 前提を確認し、`test_database_repository_contracts.py` は内部 IF 設計の Repository Port 名と構成へ修正されていることを確認した。
- [x] テスト関連成果物のディレクトリ構成が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象ファイルは `src/backend/tests/unit/`、`src/backend/tests/integration/`、`src/backend/tests/support/` のいずれかにあり、単体テスト方針、結合テスト方針、ディレクトリ構成で定義された backend テスト配置と一致している。
- [x] テスト関連成果物のファイル構成、ファイル名、配置先が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: Python テストファイルはいずれも `test_*.py` であり、設定、共通エラー、DBモデル、Repository境界、backend構成、API、migration、trace/error、support という F001 の確認対象ごとに分割されている。
- [x] 単体、結合、総合テストの役割が混ざっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストは ConfigLoader、AppError、trace_id、metadata、Repository Port/DTO、層境界を確認し、結合テストは ASGI 境界、PostgreSQL migration、trace log 書き出しを扱っている。総合テストに相当するブラウザ操作や実運用経路の確認は含まれていない。
- [x] テスト方針で求める観点、カバレッジ、証跡、実行環境、完了条件が満たされているか。
  - 検証結果: 指摘あり
  - 確認根拠: round-2 指摘の Repository Port 契約、Alembic 管理テーブル、DB 初期化、型注釈、DB 制約、trace log 必須項目はテスト本文上で補強されている。一方、設定ファイル IF と設定読込 IF で必須とされる `generator.workdir`、`validator.home`、`validator.workdir`、`validator.output_schema`、`codex_docker.image`、`trace_log.max_files_per_day` などの欠落時エラー確認が `test_config_loader.py` に残っていないことを確認した。
  - 指摘: `.issue/implement-from-docs/2026-06-21_15-21-16_設定読込テストが必須設定の欠落検証を一部網羅していない.md`
- [x] テスト方針が実装や設計の現状に追従していない場合は、テスト方針側を直す方針を書く。
  - 検証結果: 指摘なし
  - 確認根拠: 今回の確認で、単体テスト方針、結合テスト方針、静的テスト方針側を修正すべき不整合は見つからなかった。round-2 の問題はテストコード側で修正済みである。

## 単体テスト

- [x] 公開関数、公開メソッド、コンポーネント、純粋ロジックの責務を単位としているか。
  - 検証結果: 指摘なし
  - 確認根拠: `ConfigLoader.load`、`AppError` / `ErrorType` / `new_trace_id`、SQLAlchemy metadata、Repository Port/DTO、backend 層境界という F001 の公開契約を単位に確認している。
- [x] 外部副作用を Fake、Stub、Mock、fixture で差し替えているか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストでは DB 接続、HTTP 実通信、Codex 実行を行わず、`tmp_path` と `create_foundation_config` による一時ファイルで設定読込契約を確認している。DB 実体は結合テスト側に分離されている。
- [x] 事前条件、事後条件、不変条件、正常系、異常系、境界値、分岐が確認されているか。
  - 検証結果: 指摘あり
  - 確認根拠: DB metadata では列、状態/locator CHECK、型、nullable、DEFAULT、PK、FK、UNIQUE、INDEX を確認している。設定読込では欠落検証、正数制約、タイムゾーン不正、Codex Docker image 空文字、ホーム/スキーマ/ログ出力先不備を確認しているが、設定ファイル IF の必須項目全体に対する欠落時エラーの分岐確認としては不足が残る。
  - 指摘: `.issue/implement-from-docs/2026-06-21_15-21-16_設定読込テストが必須設定の欠落検証を一部網羅していない.md`
- [x] テストが内部実装詳細に密結合しすぎていないか。
  - 検証結果: 指摘なし
  - 確認根拠: metadata、AST import、Protocol import の確認は F001 の構造契約を検証する目的で使われており、設計で定義された配置・境界・DB契約の確認として妥当な範囲に収まっている。
- [x] テストが単に例外が出ないことではなく、仕様上意味のある結果、状態変化、副作用、出力を検証しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 各テストは Enum 値、AppError の分類と trace、設定属性、DB metadata 契約、API 応答 payload、DB 制約違反、trace log YAML 内容など、仕様上意味のある観測点を assert している。

## 結合テスト

- [x] 外部インターフェース、処理設計、永続化、ファイル、通信境界、表示または操作連携を設計単位で確認しているか。
  - 検証結果: 指摘なし
  - 確認根拠: `/api/app-config` は有効 Cookie 時の公開設定、UI 未設定、Cookie なし/不正 Cookie の 401 を確認している。migration は Alembic 適用、業務テーブル、`alembic_version`、主要制約、索引、制約違反を確認している。trace/error 境界は 500 応答、trace_id、YAML ログ必須項目を確認している。
- [x] 実外部サービス、実行環境、実行時間や環境差分が大きい対象を不必要に使っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 結合テストで使う実体は ASGITransport と PostgreSQL テスト DB であり、結合テスト方針の範囲内である。実 Codex、実ブラウザ、社外ネットワークは使っていない。
- [x] Mock、Fake、Stub が過剰で、実際の契約違反や結合不備を隠していないか。
  - 検証結果: 指摘なし
  - 確認根拠: app-config の認証は FastAPI dependency override で Cookie 判定を差し替えているが、依存関係を通らない実装では missing/invalid Cookie テストが失敗するため、保護対象 API の契約違反を隠していない。migration と trace/error は実際の migration 適用、ASGI 呼び出し、ファイル出力を観測している。
- [x] トランザクション、状態更新、競合、ロールバック、エラー変換、ログ依頼が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: migration テストで未完了 run の部分 UNIQUE 制約、user_state CHECK、token_hash/session_id/表示順/storage_path 重複などの IntegrityError を確認している。trace/error テストでは予期しない API 例外が 500 と trace log に変換されることを確認している。
- [x] テストデータが独立し、実行順序に依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: `prepare_foundation_database` が各 DB 結合テストの冒頭で public schema を drop/create して migration を適用する構成になっており、固定 ID を使うテストでも前回実行や他テストの残存データに依存しない。
- [x] 時刻、乱数、実行順序、外部通信、ファイルシステム、DB 状態により不安定になるテストになっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: DB テストは固定日時・固定 UUID を使用し、DB 初期化後に migration を適用している。Alembic 管理テーブルは存在確認に変わっており、全件一致による false negative は解消されている。
- [x] テストデータが実装ロジックのコピーや都合のよい値だけになっておらず、境界値、異常値、業務上意味のある値を含んでいるか。
  - 検証結果: 指摘なし
  - 確認根拠: 設定テストは欠落、空文字、不正タイムゾーン、正数境界違反、実体不足を含み、DB テストは未定義状態、重複、一意制約、表示順 0 など設計上意味のある異常値を含んでいる。

## 総合テスト

- [x] 利用者が利用者インターフェースまたは実運用に近い経路で業務を完了できることを確認しているか。
  - 検証結果: 対象外
  - 確認根拠: 今回のレビュー対象は F001 の単体テスト、結合テスト、補助ファイルであり、総合テスト仕様・結果やブラウザ経路の成果物は含まれていない。
  - 理由: 現在フェーズがテスト検証であり、生成担当の作成物が単体・結合テストコードに限定されているため。
- [x] 自動確認、手動確認、通信確認、永続化確認、ファイル確認の役割がテスト方針と一致しているか。
  - 検証結果: 対象外
  - 確認根拠: 総合テスト成果物はレビュー対象外であり、今回の確認対象に手動確認、Playwright、実運用経路の証跡は含まれていない。
  - 理由: F001 round-3 の対象が backend 単体・結合テストコードであるため。
- [x] 未実施、一部確認、保留、不合格、再テストの記録がテスト仕様・結果に残っているか。
  - 検証結果: 対象外
  - 確認根拠: 総合テスト仕様・結果ファイルはレビュー対象に含まれていない。現時点で参照した実行結果は生成役報告の Red 要約である。
  - 理由: 総合テスト記録のレビューではないため。
- [x] 実行証跡とテストケースが追跡できるか。
  - 検証結果: 対象外
  - 確認根拠: 総合テストの実行証跡は今回の作成物ではない。単体・結合テストの Red 要約は生成役報告にあるが、総合テストケースとの追跡性を判定する対象ではない。
  - 理由: F001 の総合テストフェーズではないため。

## TDD と実装順序

- [x] 実装成果物レビューでは、TDD が要求される範囲で Red、Green、Refactor の証跡または説明があるか。
  - 検証結果: 指摘なし
  - 確認根拠: state の Red 確認結果では、単体 `34 failed`、結合 `9 failed` の Red が示されている。現フェーズはテスト検証であり、Green/Refactor は未実施として扱われている。
- [x] Red を事後的に作れない場合は、レビュー指摘として記録し、今後の修正方針を示す。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告に Red 確認結果があり、対象テスト本文も本体未実装で失敗する契約テストとして構成されている。Red を事後的に作れない状態ではない。
- [x] テスト追加が不要な場合は、除外理由が方針と整合しているか。
  - 検証結果: 対象外
  - 確認根拠: 今回は F001 のテストコード修正・追加が行われており、「テスト追加不要」の判断は行われていない。
  - 理由: テスト追加不要ケースではないため。

## 不要なコメントと作業経緯

- [x] テスト名、docstring、コメント、テスト仕様、証跡に、確認対象の理解や再実行に不要な作業経緯、内部事情、言い訳、暫定理由が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テストの docstring は各テストで「観点」「確認」を記載しており、作成経緯、暫定理由、レビュー反応、環境制約の説明は混入していない。
- [x] コメントや docstring は、仕様上の意図、契約、前提条件、非自明な制約の説明に限定されているか。
  - 検証結果: 指摘なし
  - 確認根拠: docstring は設定読込、共通エラー、trace_id、DBモデル、Repository境界、API公開範囲、migration、trace log など仕様上の確認対象と期待結果を説明している。
- [x] テスト失敗理由、作成時の都合、環境制約、ツール制約、レビュー指摘への反応など、成果物ではなく作業報告や issue に書くべき内容が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テスト本文と support には Red 失敗理由、作業都合、ツール制約などの作業報告は含まれていない。生成役の直接 `python -m py_compile` 誤実行は報告事項に留まり、成果物本文には混入していない。
- [x] `既存実装があるので失敗しない`、`一旦この実装にしている`、`あとで修正する`、`指摘されたため追加`、`カバレッジのため追加`、`環境の都合で確認できない`、`旧仕様ではこうだった`、コードの単なる日本語言い換えなどが残っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象ファイル本文を確認し、列挙された暫定表現や作業経緯表現は含まれていない。docstring は確認対象と期待結果に限定されている。
- [x] 業務ルール上の非自明な前提、セキュリティ上の制約、外部仕様に由来する制約、特定テストデータが必要な理由、mock/fake/stub の境界理由、並行実行、時刻、ID、ファイル、通信の注意点など、確認や保守に必要な説明は残されているか。
  - 検証結果: 指摘なし
  - 確認根拠: 各テストの「観点」「確認」により、認証 Cookie、内部設定の非公開、未完了 run 部分 UNIQUE、Alembic version table、trace log 必須項目など、保守に必要な前提を把握できる。

## 修正方針の判断

- [x] テスト不足ならテストコード、テスト仕様、証跡のどれを補うかを書く。
  - 検証結果: 指摘あり
  - 確認根拠: round-2 のテスト不足・契約誤り 8 件のうち、設定読込の必須項目欠落検証に一部不足が残っている。これはテスト仕様や証跡ではなく、`test_config_loader.py` の欠落検証ケースを補うべき問題として整理した。
  - 指摘: `.issue/implement-from-docs/2026-06-21_15-21-16_設定読込テストが必須設定の欠落検証を一部網羅していない.md`
- [x] テストが仕様に従っていて実装が違う場合は、実装側を直す方針を書く。
  - 検証結果: 指摘なし
  - 確認根拠: 現時点では本実装未実施であり、今回の確認対象はテストコードである。テストが仕様に従い、既存実装差異を指摘する状況ではない。
- [x] テスト方針が過剰または古い場合は、テスト方針と関連成果物を直す方針を書く。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テスト方針、結合テスト方針、静的テスト方針、コーディング規約の内容は F001 のレビュー観点として有効であり、テスト方針修正が必要な箇所は確認していない。

## 既存 issue 解消判定

- `.issue/implement-from-docs/2026-06-21_15-12-01_アプリ設定取得テストが認証前提を確認していない.md`
  - 判定: 解消済み
  - 確認根拠: `test_app_config_api.py` が有効 Cookie 付き 200 と Cookie なし/不正 Cookie 401 を確認するようになっている。
- `.issue/implement-from-docs/2026-06-21_15-12-02_Repository契約テストが内部IF設計と一致しない.md`
  - 判定: 解消済み
  - 確認根拠: `test_database_repository_contracts.py` が `DeleteChatRepositoryPort`、`TransactionManagerPort`、`AcceptedRunStateRepositoryPort`、`RecoveryRepositoryPort`、`UnfinishedRun` を確認対象に含めている。
- `.issue/implement-from-docs/2026-06-21_15-12-03_マイグレーションテストがAlembic管理テーブルで誤失敗する.md`
  - 判定: 解消済み
  - 確認根拠: `test_database_migrations.py` は業務テーブルの存在を個別確認し、`alembic_version` は存在確認へ変更されている。
- `.issue/implement-from-docs/2026-06-21_15-12-04_結合DBテストがDB状態を初期化していない.md`
  - 判定: 解消済み
  - 確認根拠: `foundation.py` の `prepare_foundation_database` が public schema を reset してから migration を適用し、各 DB 結合テストが冒頭で呼び出している。
- `.issue/implement-from-docs/2026-06-21_15-12-05_マイグレーションテストのヘルパ引数型が未注釈.md`
  - 判定: 解消済み
  - 確認根拠: `test_database_migrations.py` の inspect helper は `Inspector` 型注釈を持つようになっている。
- `.issue/implement-from-docs/2026-06-21_15-12-06_設定読込テストが必須検証項目を網羅していない.md`
  - 判定: 未解消
  - 確認根拠: `test_config_loader.py` は必須項目欠落、タイムゾーン不正、Codex Docker image 空文字、生成/検証ホームとスキーマ不備、trace_log.dir 不備を補強しているが、設定ファイル IF で必須とされる一部項目の欠落時エラー確認が残っていない。
  - 指摘: `.issue/implement-from-docs/2026-06-21_15-21-16_設定読込テストが必須設定の欠落検証を一部網羅していない.md`
- `.issue/implement-from-docs/2026-06-21_15-12-07_DBスキーマテストが主要制約を網羅していない.md`
  - 判定: 解消済み
  - 確認根拠: DB metadata と migration テストが列型、NULL 可否、DEFAULT、PK、FK、UNIQUE、CHECK、通常 INDEX、部分 UNIQUE、代表的な制約違反を確認するよう補強されている。
- `.issue/implement-from-docs/2026-06-21_15-12-08_トレースログ境界テストが必須項目を確認していない.md`
  - 判定: 解消済み
  - 確認根拠: `test_trace_error_boundary.py` が `occurred_at`、関連 ID 欄、`exception_type`、`stacktrace`、HTTP 情報、`status_code` を YAML ログの必須確認に含めている。
