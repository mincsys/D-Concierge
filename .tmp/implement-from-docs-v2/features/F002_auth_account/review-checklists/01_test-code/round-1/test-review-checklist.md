# Test Review Checklist

## テスト方針との整合

- [x] テストコードとテスト仕様が、対応するテスト方針の範囲指定、テスト対象単位、除外範囲に一致しているか。
  - 検証結果: 指摘あり
  - 確認根拠: 単体・結合テスト方針は本フェーズを単体・結合テストコード作成範囲とし、F002 の依頼前提ではアカウント削除受付を F002、物理削除・回復を F007 へ寄せることが明示されている。一方で `test_account_management_use_cases.py` は `ExecuteAccountDeletionUseCase` と `StartupAccountRecoveryUseCase`、`test_account_management_api.py` は起動時アカウント回復を F002 レビュー対象としているため、F002 の範囲指定を超えている。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-01-01_F002テストがF007の物理削除と回復まで検証している.md`
- [x] テスト関連成果物のディレクトリ構成が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F002 の単体テストは `src/backend/tests/unit/application/account/`、Port 契約は `src/backend/tests/unit/application/ports/`、security 具象は `src/backend/tests/unit/infrastructure/security/`、結合テストは `src/backend/tests/integration/`、補助コードは `src/backend/tests/support/` に配置され、単体・結合テスト方針の配置規則と一致している。
- [x] テスト関連成果物のファイル構成、ファイル名、配置先が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘あり
  - 確認根拠: 対象テストは `test_*.py` 命名で、認証、アカウント管理、Port、security、API、DB契約の単位ごとに分割されている。配置先も pytest の標準検出規則とバックエンドテスト方針に沿っている。一方で同じ認証ユースケース群を `test_register_and_login_use_cases.py`、`test_authenticate_and_logout_use_cases.py`、`test_auth_account_use_cases.py` が重複して扱い、公開 `execute` 契約とコンストラクタ契約が一致していないため、テストファイル分割が契約の一貫性を崩している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
- [x] 単体、結合、総合テストの役割が混ざっていないか。
  - 検証結果: 指摘あり
  - 確認根拠: 総合テストは含まれていないが、F002 単体・結合テストの中に F007 相当の物理削除、ファイル削除、起動時回復の検証が混入している。これは F002 のアカウント削除受付テストと後続機能テストの役割を混ぜている。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-01-01_F002テストがF007の物理削除と回復まで検証している.md`
- [x] テスト方針で求める観点、カバレッジ、証跡、実行環境、完了条件が満たされているか。
  - 検証結果: 指摘あり
  - 確認根拠: 認証登録、ログイン、認証状態、ログアウト、アカウント削除受付の主要観点はあるが、認証 API の主要異常系、ユーザ名入力不正、新しいパスワード入力不正の分岐が不足している。また同一ユースケースの公開契約がテスト間で二重定義され、`test_auth_account_api.py` の `_field_errors_payload(field_errors: dict)` は広すぎる辞書型で、テスト方針と AGENTS.md の型制約に反している。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
    - `.issue/implement-from-docs/2026-06-21_17-30-02_認証API結合テストが主要異常系REST契約を網羅していない.md`
    - `.issue/implement-from-docs/2026-06-21_17-01-02_アカウント管理テストが入力不正の必須分岐を網羅していない.md`
    - `.issue/implement-from-docs/2026-06-21_17-01-03_field_errorsヘルパが広すぎるdict型を使っている.md`
- [x] テスト方針が実装や設計の現状に追従していない場合は、テスト方針側を直す方針を書く。
  - 検証結果: 指摘なし
  - 確認根拠: 確認した不足と境界違反はテストコード側の範囲・網羅・型注釈の問題であり、単体テスト方針や結合テスト方針を修正すべき乖離は確認していない。

## 単体テスト

- [x] 公開関数、公開メソッド、コンポーネント、純粋ロジックの責務を単位としているか。
  - 検証結果: 指摘あり
  - 確認根拠: F002 の登録、ログイン、認証確認、ログアウト、ユーザ名変更、パスワード変更、削除受付は公開 UseCase/Port 単位で確認されている。一方で同一 UseCase の `execute` を Command オブジェクト方式とキーワード引数方式の両方で要求している。また `ExecuteAccountDeletionUseCase` と `StartupAccountRecoveryUseCase` は F002 の公開責務ではなく、F007 へ寄せるべき責務としてテスト対象に含まれている。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
    - `.issue/implement-from-docs/2026-06-21_17-01-01_F002テストがF007の物理削除と回復まで検証している.md`
- [x] 外部副作用を Fake、Stub、Mock、fixture で差し替えているか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストは `FakeAccountRepository`、`FakePasswordHasher`、`FakeSessionTokenProvider`、`FakeAccountDeletionDispatcher`、`FakeTraceLogger`、`FakeTransactionManager`、`FixedClock` を使い、DB、時刻、ハッシュ、トークン、トレースログ、dispatcher の副作用を差し替えている。
- [x] 事前条件、事後条件、不変条件、正常系、異常系、境界値、分岐が確認されているか。
  - 検証結果: 指摘あり
  - 確認根拠: 登録、ログイン、認証状態、ログアウト、パスワード現在値不一致、削除受付の代表分岐は確認されているが、ユーザ名入力不正、新しいパスワード入力不正、新しいパスワード確認不一致の単体分岐が不足している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-01-02_アカウント管理テストが入力不正の必須分岐を網羅していない.md`
- [x] テストが内部実装詳細に密結合しすぎていないか。
  - 検証結果: 指摘なし
  - 確認根拠: ユースケーステストは公開コマンド、戻り値、Fake Port の呼び出し結果を観測しており、SQLAlchemy モデルや具象内部手順には直接依存していない。Port 契約テストも Protocol/DTO の公開契約を確認している。
- [x] テストが単に例外が出ないことではなく、仕様上意味のある結果、状態変化、副作用、出力を検証しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 各テストは `field_errors`、Cookie 用生トークンと保存用 hash の分離、セッション削除、DB 状態、Cookie 属性、レスポンス payload、トレースログ依頼など、仕様上の観測点を assert している。

## 結合テスト

- [x] 外部インターフェース、処理設計、永続化、ファイル、通信境界、表示または操作連携を設計単位で確認しているか。
  - 検証結果: 指摘あり
  - 確認根拠: IF-SB-11 から IF-SB-17 の主要 API、Cookie、DB 永続化は確認されているが、IF-SB-11 から IF-SB-14 の主要異常系、IF-SB-15 のユーザ名入力不正、IF-SB-16 の新しいパスワード入力不正を API 境界で確認していない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-21_17-30-02_認証API結合テストが主要異常系REST契約を網羅していない.md`
    - `.issue/implement-from-docs/2026-06-21_17-01-02_アカウント管理テストが入力不正の必須分岐を網羅していない.md`
- [x] 実外部サービス、実行環境、実行時間や環境差分が大きい対象を不必要に使っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 結合テストは FastAPI ASGITransport と PostgreSQL テストDB、SQLAlchemy、pytest fixture を使っており、実Codex、実ブラウザ、社外サービス、本番データには依存していない。
- [x] Mock、Fake、Stub が過剰で、実際の契約違反や結合不備を隠していないか。
  - 検証結果: 指摘なし
  - 確認根拠: API 結合テストは `create_app`、ASGITransport、実DB、実 password hasher/token provider を経由しており、F002 の HTTP、Cookie、DB 結合契約を Fake で隠していない。
- [x] トランザクション、状態更新、競合、ロールバック、エラー変換、ログ依頼が確認されているか。
  - 検証結果: 指摘あり
  - 確認根拠: 削除受付やセッション置換の状態更新、入力エラー変換は一部確認されているが、F002 の結合テストに F007 の起動時回復が含まれている。また認証 API の主要異常系、ユーザ名入力不正、新しいパスワード入力不正のエラー変換が不足している。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-21_17-30-02_認証API結合テストが主要異常系REST契約を網羅していない.md`
    - `.issue/implement-from-docs/2026-06-21_17-01-01_F002テストがF007の物理削除と回復まで検証している.md`
    - `.issue/implement-from-docs/2026-06-21_17-01-02_アカウント管理テストが入力不正の必須分岐を網羅していない.md`
- [x] テストデータが独立し、実行順序に依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 結合テストは各テストで `prepare_foundation_database(database_url)` を呼び、必要なユーザ、セッション、チャットを個別に seed しているため、テスト間の実行順序に依存しない構成になっている。
- [x] 時刻、乱数、実行順序、外部通信、ファイルシステム、DB 状態により不安定になるテストになっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストは `FixedClock` と固定 UUID/token を使い、結合テストも固定時刻で seed している。DB は各テストで初期化され、外部通信や実ブラウザを使っていない。
- [x] テストデータが実装ロジックのコピーや都合のよい値だけになっておらず、境界値、異常値、業務上意味のある値を含んでいるか。
  - 検証結果: 指摘なし
  - 確認根拠: 登録の不正ユーザID、空ユーザ名、短いパスワード、確認不一致、ログインの不存在/deleting/不一致、期限切れセッション、Cookieなし、削除受付など、業務上意味のある値を含めている。

## 総合テスト

- [x] 利用者が利用者インターフェースまたは実運用に近い経路で業務を完了できることを確認しているか。
  - 検証結果: 対象外
  - 確認根拠: 今回のレビュー対象は F002 の単体・結合テストコードであり、機能別総合テスト仕様や Playwright/手動確認は対象に含まれていない。
  - 理由: 総合テストフェーズではないため。
- [x] 自動確認、手動確認、通信確認、永続化確認、ファイル確認の役割がテスト方針と一致しているか。
  - 検証結果: 対象外
  - 確認根拠: F002 テストコードレビュー round-1 では単体・結合テストコードのみを確認し、総合テストの自動/手動/証跡分類は扱わない。
  - 理由: 総合テストフェーズではないため。
- [x] 未実施、一部確認、保留、不合格、再テストの記録がテスト仕様・結果に残っているか。
  - 検証結果: 対象外
  - 確認根拠: テスト仕様・結果ファイルは今回のレビュー対象ではなく、生成役報告の Red 結果と `state.md` の記録を確認対象にしている。
  - 理由: 総合テスト結果レビューではないため。
- [x] 実行証跡とテストケースが追跡できるか。
  - 検証結果: 対象外
  - 確認根拠: 今回は実行系コマンド禁止で、証跡生成や総合テスト証跡照合は対象外である。Red 結果は生成役報告で確認した。
  - 理由: 総合テスト証跡レビューではないため。

## TDD と実装順序

- [x] 実装成果物レビューでは、TDD が要求される範囲で Red、Green、Refactor の証跡または説明があるか。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告に単体 `34 failed, 70 passed`、結合 `11 failed, 30 passed`、失敗理由が F002 未実装、F002 API 未登録、security 実装未作成に起因することが記録されている。今回のフェーズはテストコード先行レビューであり、Green/Refactor は後続実装フェーズの対象である。
- [x] Red を事後的に作れない場合は、レビュー指摘として記録し、今後の修正方針を示す。
  - 検証結果: 指摘なし
  - 確認根拠: 生成役報告と `state.md` では Red が成立しており、Red を作れない対象として扱う必要はない。
- [x] テスト追加が不要な場合は、除外理由が方針と整合しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F002 はテストコード先行作成フェーズであり、テスト追加不要の判断は提示されていない。不足しているテスト観点は issue として記録した。

## 不要なコメントと作業経緯

- [x] テスト名、docstring、コメント、テスト仕様、証跡に、確認対象の理解や再実行に不要な作業経緯、内部事情、言い訳、暫定理由が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テストの docstring は `観点` と `確認` で構成され、Red 理由、未実装理由、レビュー対応経緯、環境都合などの作業報告は混入していない。
- [x] コメントや docstring は、仕様上の意図、契約、前提条件、非自明な制約の説明に限定されているか。
  - 検証結果: 指摘なし
  - 確認根拠: docstring は API IF、UseCase、Port、Cookie、DB 状態、セッション境界などの確認意図を説明しており、コードの単なる日本語言い換えにはなっていない。
- [x] テスト失敗理由、作成時の都合、環境制約、ツール制約、レビュー指摘への反応など、成果物ではなく作業報告や issue に書くべき内容が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テストと補助コードに、未実装のため失敗する、環境のため確認できない、指摘対応で追加した、といった作業経緯の記述は確認していない。
- [x] `既存実装があるので失敗しない`、`一旦この実装にしている`、`あとで修正する`、`指摘されたため追加`、`カバレッジのため追加`、`環境の都合で確認できない`、`旧仕様ではこうだった`、コードの単なる日本語言い換えなどが残っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象ファイル内のコメント/docstring には、暫定理由、旧仕様、カバレッジ目的、環境都合のような文言は確認していない。
- [x] 業務ルール上の非自明な前提、セキュリティ上の制約、外部仕様に由来する制約、特定テストデータが必要な理由、mock/fake/stub の境界理由、並行実行、時刻、ID、ファイル、通信の注意点など、確認や保守に必要な説明は残されているか。
  - 検証結果: 指摘なし
  - 確認根拠: パスワード生値非保存、Cookie 生トークンと token_hash 分離、期限切れ/deleting セッション削除、ログアウトの現在セッション限定、アカウント削除受付の deleting 状態維持など、保守に必要な確認意図は docstring に記載されている。

## 修正方針の判断

- [x] テスト不足ならテストコード、テスト仕様、証跡のどれを補うかを書く。
  - 検証結果: 指摘なし
  - 確認根拠: テスト不足と契約不一致については、補うべき対象を issue に記録した。`2026-06-21_17-30-01` はユースケース単体テストの公開契約統一、`2026-06-21_17-30-02` は認証 API 結合テストの異常系追加、`2026-06-21_17-01-02` はアカウント管理の単体・結合テスト追加、`2026-06-21_17-01-03` はテスト補助型注釈修正、`2026-06-21_17-01-01` は F002/F007 のテスト範囲分離を修正方針として記録している。
- [x] テストが仕様に従っていて実装が違う場合は、実装側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 今回は F002 本実装前のテストコードレビューであり、実装差異の判定はしていない。
  - 理由: 実装成果物レビューではないため。
- [x] テスト方針が過剰または古い場合は、テスト方針と関連成果物を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: テスト方針の過剰または陳腐化は確認していない。
  - 理由: テスト方針修正が必要な指摘ではないため。
