# Test Review Checklist

## テスト方針との整合

- [x] テストコードとテスト仕様が、対応するテスト方針の範囲指定、テスト対象単位、除外範囲に一致しているか。
  - 検証結果: 指摘あり
  - 確認根拠: `docs/04_テスト/02_単体テスト/単体テスト方針.md` は公開メソッド単位の単体テスト、`docs/04_テスト/03_結合テスト/結合テスト方針.md` は REST API と DB 連携の結合確認を求めている。F002 対象テストは単体・結合の配置と F002 範囲におおむね一致し、F007 の物理削除・起動時回復テストは除外済みである。一方で `test_auth_account_use_cases.py` が同一ユースケースの `execute` をキーワード引数形式で再定義し、`test_auth_account_api.py` は IF-SB-11 から IF-SB-14 の主要異常系をまだ確認していない。
  - 指摘:
    - `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
    - `.issue/implement-from-docs/2026-06-21_17-30-02_認証API結合テストが主要異常系REST契約を網羅していない.md`
- [x] テスト関連成果物のディレクトリ構成が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F002 の単体テストは `src/backend/tests/unit/application/account/`、Port 契約は `src/backend/tests/unit/application/ports/`、security 具象は `src/backend/tests/unit/infrastructure/security/`、結合テストは `src/backend/tests/integration/`、補助コードは `src/backend/tests/support/` に配置され、単体・結合テスト方針の配置規則と一致している。
- [x] テスト関連成果物のファイル構成、ファイル名、配置先が、テスト方針、設計書、開発標準と一致しているか。
  - 検証結果: 指摘あり
  - 確認根拠: 対象ファイルは `test_*.py` 命名で配置先も方針に合うが、`test_register_and_login_use_cases.py`、`test_authenticate_and_logout_use_cases.py`、`test_account_management_use_cases.py` が Command オブジェクト形式の公開契約を検証する一方、`test_auth_account_use_cases.py` は同一ユースケースを Protocol 経由のキーワード引数形式として扱い、ファイル分割が公開契約の一貫性を崩している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
- [x] 単体、結合、総合テストの役割が混ざっていないか。
  - 検証結果: 指摘あり
  - 確認根拠: 総合テストは含まれておらず、F007 の物理削除・起動時回復の直接検証も削除済みである。ただし `test_auth_account_use_cases.py` は単体テスト内で同一公開契約を別の抽象 Protocol として再定義しており、既存の Command 契約テストと役割が重複している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
- [x] テスト方針で求める観点、カバレッジ、証跡、実行環境、完了条件が満たされているか。
  - 検証結果: 指摘あり
  - 確認根拠: 登録、ログイン、認証状態、ログアウト、ユーザ名変更、パスワード変更、削除受付の単体観点とアカウント管理 API の入力不正は追加されている。Red 結果も `state.md` に記録されている。一方で認証 API 結合テストでは、登録のユーザID重複、ログインのユーザID不存在、削除中ユーザ、認証状態確認の削除中ユーザ、Cookie なしログアウトの REST 契約確認が不足している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-02_認証API結合テストが主要異常系REST契約を網羅していない.md`
- [x] テスト方針が実装や設計の現状に追従していない場合は、テスト方針側を直す方針を書く。
  - 検証結果: 指摘なし
  - 確認根拠: 確認した問題はテストコード側の公開契約重複と認証 API 異常系不足であり、単体テスト方針、結合テスト方針、関連設計を修正すべき乖離は確認していない。

## 単体テスト

- [x] 公開関数、公開メソッド、コンポーネント、純粋ロジックの責務を単位としているか。
  - 検証結果: 指摘あり
  - 確認根拠: `test_register_and_login_use_cases.py`、`test_authenticate_and_logout_use_cases.py`、`test_account_management_use_cases.py` は各 UseCase の `execute(Command)` を単位に戻り値、状態変化、副作用を確認している。一方で `test_auth_account_use_cases.py` は `RegisterAccountExecutor` などの Protocol を定義し、同じ UseCase に `execute(user_id=..., trace_id=...)` を要求している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
- [x] 外部副作用を Fake、Stub、Mock、fixture で差し替えているか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストは `FakeAccountRepository`、`FakePasswordHasher`、`FakeSessionTokenProvider`、`FakeAccountDeletionDispatcher`、`FakeTraceLogger`、`FakeTransactionManager`、`FixedClock` を使い、DB、時刻、ハッシュ、トークン、dispatcher、トレースログの副作用を差し替えている。
- [x] 事前条件、事後条件、不変条件、正常系、異常系、境界値、分岐が確認されているか。
  - 検証結果: 指摘なし
  - 確認根拠: `test_register_and_login_use_cases.py` は登録入力不正、重複、ログイン不存在、削除中ユーザ、パスワード不一致を確認している。`test_authenticate_and_logout_use_cases.py` は Cookie なし、期限切れ、削除中ユーザ、ログアウト Cookie なしを確認し、`test_account_management_use_cases.py` はユーザ名不正、新パスワード不正、確認不一致、削除受付失敗時の維持を確認している。
- [x] テストが内部実装詳細に密結合しすぎていないか。
  - 検証結果: 指摘あり
  - 確認根拠: Command 形式のテストは公開入力・戻り値・Port 副作用を観測しており、SQLAlchemy や具象実装詳細には密結合していない。一方で `test_auth_account_use_cases.py` は設計側の正式契約とは別にテスト専用 Protocol とキーワード引数 `execute` を公開契約として置いており、実装者に余分な互換 API を要求する。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-01_ユースケーステストが公開execute契約を二重定義している.md`
- [x] テストが単に例外が出ないことではなく、仕様上意味のある結果、状態変化、副作用、出力を検証しているか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テストは `field_errors`、Cookie 用生トークンと保存用 `token_hash` の分離、セッション削除、ユーザ・チャットの `deleting` 更新、DB 状態、Cookie 属性、レスポンス payload、トレースログ依頼など、仕様上の観測点を assert している。

## 結合テスト

- [x] 外部インターフェース、処理設計、永続化、ファイル、通信境界、表示または操作連携を設計単位で確認しているか。
  - 検証結果: 指摘あり
  - 確認根拠: `test_auth_account_api.py` は登録正常、登録入力不正、ログイン正常、ログインのパスワード不一致、認証状態の期限切れ、ログアウト正常を確認している。しかし IF-SB-12 のユーザID重複、IF-SB-13 のユーザID不存在と削除中ユーザ、IF-SB-11 の削除中ユーザ、IF-SB-14 の Cookie なしログアウトが不足している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-02_認証API結合テストが主要異常系REST契約を網羅していない.md`
- [x] 実外部サービス、実行環境、実行時間や環境差分が大きい対象を不必要に使っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 結合テストは FastAPI `ASGITransport`、PostgreSQL テスト DB、SQLAlchemy、pytest fixture を使う構成で、実 Codex、実ブラウザ、社外サービス、本番データには依存していない。
- [x] Mock、Fake、Stub が過剰で、実際の契約違反や結合不備を隠していないか。
  - 検証結果: 指摘なし
  - 確認根拠: API 結合テストは `create_app`、ASGITransport、実 DB、実 password hasher/token provider を経由しており、F002 の HTTP、Cookie、DB 結合契約を Fake で隠していない。
- [x] トランザクション、状態更新、競合、ロールバック、エラー変換、ログ依頼が確認されているか。
  - 検証結果: 指摘あり
  - 確認根拠: `test_account_management_api.py` はユーザ名・パスワード・削除受付の DB 非更新または状態更新を確認している。`test_auth_account_api.py` ではパスワード不一致や期限切れのエラー変換はあるが、ユーザID重複、ユーザID不存在、削除中ユーザ、Cookie なしログアウトのエラー変換と状態維持が不足している。
  - 指摘: `.issue/implement-from-docs/2026-06-21_17-30-02_認証API結合テストが主要異常系REST契約を網羅していない.md`
- [x] テストデータが独立し、実行順序に依存していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 結合テストは各テストで `prepare_foundation_database(database_url)` を呼び、必要なユーザ、セッション、チャットを個別に seed しているため、テスト間の実行順序に依存しない構成になっている。
- [x] 時刻、乱数、実行順序、外部通信、ファイルシステム、DB 状態により不安定になるテストになっていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 単体テストは `FixedClock` と固定 token/hash を使い、結合テストも固定日時と固定 UUID を seed している。DB は各テストで初期化され、外部通信や実ブラウザを使っていない。
- [x] テストデータが実装ロジックのコピーや都合のよい値だけになっておらず、境界値、異常値、業務上意味のある値を含んでいるか。
  - 検証結果: 指摘なし
  - 確認根拠: 登録の不正ユーザID、空ユーザ名、短いパスワード、確認不一致、ログインの不存在/deleting/不一致、期限切れセッション、Cookie なし、削除受付など、業務上意味のある値を含めている。

## 総合テスト

- [x] 利用者が利用者インターフェースまたは実運用に近い経路で業務を完了できることを確認しているか。
  - 検証結果: 対象外
  - 確認根拠: 今回のレビュー対象は F002 の単体・結合テストコードであり、機能別総合テスト仕様や Playwright/手動確認は対象に含まれていない。
  - 理由: 総合テストフェーズではないため。
- [x] 自動確認、手動確認、通信確認、永続化確認、ファイル確認の役割がテスト方針と一致しているか。
  - 検証結果: 対象外
  - 確認根拠: F002 テストコードレビュー round-2 では単体・結合テストコードのみを確認し、総合テストの自動/手動/証跡分類は扱わない。
  - 理由: 総合テストフェーズではないため。
- [x] 未実施、一部確認、保留、不合格、再テストの記録がテスト仕様・結果に残っているか。
  - 検証結果: 対象外
  - 確認根拠: テスト仕様・結果ファイルは今回のレビュー対象ではなく、生成役報告と `state.md` の Red 結果を確認対象にした。
  - 理由: 総合テスト結果レビューではないため。
- [x] 実行証跡とテストケースが追跡できるか。
  - 検証結果: 対象外
  - 確認根拠: 今回は実行系コマンド禁止で、証跡生成や総合テスト証跡照合は対象外である。Red 結果は `state.md` に記録されていることを確認した。
  - 理由: 総合テスト証跡レビューではないため。

## TDD と実装順序

- [x] 実装成果物レビューでは、TDD が要求される範囲で Red、Green、Refactor の証跡または説明があるか。
  - 検証結果: 指摘なし
  - 確認根拠: `state.md` にはテスト修正 round-1 後の Red 結果として単体 `35 failed, 70 passed`、結合 `13 failed, 30 passed`、失敗理由が F002 未実装、Port/DTO 未拡張、認証/account API 未登録に起因することが記録されている。今回のフェーズはテストコード先行レビューであり、Green/Refactor は後続実装フェーズの対象である。
- [x] Red を事後的に作れない場合は、レビュー指摘として記録し、今後の修正方針を示す。
  - 検証結果: 指摘なし
  - 確認根拠: `state.md` の Red 確認では DB 接続成功後に F002 未実装起因の失敗へ切り分けられており、Red を作れない対象として扱う必要はない。
- [x] テスト追加が不要な場合は、除外理由が方針と整合しているか。
  - 検証結果: 指摘なし
  - 確認根拠: F002 はテストコード先行作成フェーズであり、テスト追加不要の判断は提示されていない。不足している認証 API 異常系は既存 issue として残っている。

## 不要なコメントと作業経緯

- [x] テスト名、docstring、コメント、テスト仕様、証跡に、確認対象の理解や再実行に不要な作業経緯、内部事情、言い訳、暫定理由が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テストの docstring は `観点` と `確認` で構成され、Red 理由、未実装理由、レビュー対応経緯、環境都合などの作業報告は混入していない。
- [x] コメントや docstring は、仕様上の意図、契約、前提条件、非自明な制約の説明に限定されているか。
  - 検証結果: 指摘なし
  - 確認根拠: docstring は API IF、UseCase、Port、Cookie、DB 状態、セッション境界などの確認意図を説明しており、コードの単なる日本語言い換えにはなっていない。
- [x] テスト失敗理由、作成時の都合、環境制約、ツール制約、レビュー指摘への反応など、成果物ではなく作業報告や issue に書くべき内容が混入していないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象テストと補助コードに、未実装のため失敗する、環境のため確認できない、指摘対応で追加した、といった作業経緯の記述は確認していない。
- [x] `既存実装があるので失敗しない`、`一旦この実装にしている`、`あとで修正する`、`指摘されたため追加`、`カバレッジのため追加`、`環境の都合で確認できない`、`旧仕様ではこうだった`、コードの単なる日本語言い換えなどが残っていないか。
  - 検証結果: 指摘なし
  - 確認根拠: 対象ファイル内のコメント/docstring には、暫定理由、旧仕様、カバレッジ目的、環境都合のような文言は確認していない。
- [x] 業務ルール上の非自明な前提、セキュリティ上の制約、外部仕様に由来する制約、特定テストデータが必要な理由、mock/fake/stub の境界理由、並行実行、時刻、ID、ファイル、通信の注意点など、確認や保守に必要な説明は残されているか。
  - 検証結果: 指摘なし
  - 確認根拠: パスワード生値非保存、Cookie 生トークンと `token_hash` 分離、期限切れ/deleting セッション削除、ログアウトの現在セッション限定、アカウント削除受付の deleting 状態維持など、保守に必要な確認意図は docstring に記載されている。

## 修正方針の判断

- [x] テスト不足ならテストコード、テスト仕様、証跡のどれを補うかを書く。
  - 検証結果: 指摘なし
  - 確認根拠: テスト不足と契約不一致については、既存 issue に補うべき対象が記録されている。`2026-06-21_17-30-01` はユースケース単体テストの公開契約統一、`2026-06-21_17-30-02` は認証 API 結合テストの異常系追加を求めており、今回解消済みの 3 件は削除可 issue として分類した。
- [x] テストが仕様に従っていて実装が違う場合は、実装側を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: 今回は F002 本実装前のテストコードレビューであり、実装差異の判定はしていない。
  - 理由: 実装成果物レビューではないため。
- [x] テスト方針が過剰または古い場合は、テスト方針と関連成果物を直す方針を書く。
  - 検証結果: 対象外
  - 確認根拠: テスト方針の過剰または陳腐化は確認していない。
  - 理由: テスト方針修正が必要な指摘ではないため。
