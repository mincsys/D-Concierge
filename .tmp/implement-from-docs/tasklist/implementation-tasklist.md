# 実装タスクリスト

## 対象範囲と前提確認

- [x] `implement-from-docs` スキルの手順を確認する。
- [x] `AGENTS.md` とプロジェクト指示を確認する。
- [x] `docs/01_要件定義/` からMVP対象範囲、対象外範囲、受け入れ条件の概要を確認する。
- [x] `docs/02_外部設計/` から機能一覧、状態、主要IF、画面対象の概要を確認する。
- [x] `docs/03_内部設計/` からレイヤ構成、ディレクトリ構成、処理一覧の概要を確認する。
- [x] `docs/04_テスト/` から静的・単体・結合テストの方針、コマンド、カバレッジ基準を確認する。
- [x] `docs/05_開発標準/` からTDD、型、コメント、依存境界、実行コマンドの規約を確認する。
- [x] 既存 `.issue/` を確認する。
- [x] 現行実装の配置を確認する。
- [x] 実装対象、対象外、完了判定基準をユーザへ質問して確定する。
  - 対象範囲: MVP全体。
- [x] 既存 `.issue/` を今回対象に含めるか確認する。
  - `.issue/compose-service-name-mismatch.md` は今回対象外。
- [x] 未解消 `.issue/`、未反映の任意成果物、設計補助資料の扱いを確認する。
  - `docs/00_設計・技術検証メモ/` と将来構想は参考扱い。

## 設計紐づけ

- [x] 対象実装に対応する要件を確認する。
- [x] 対象実装に対応する外部IF、画面、データ、メッセージ、ログを確認する。
- [x] 対象実装に対応する内部設計、クラス設計、処理設計、物理データ設計、共通設計を確認する。
- [x] 対象実装に対応する静的テスト、単体テスト、結合テスト、総合テストの方針を確認する。
- [x] 対象実装に対応するコーディング規約と実行コマンドを確認する。

## 契約マトリクス

- [x] 単体テストで確認する契約を `.tmp/implement-from-docs/contract-matrix/unit/` に作成する。
- [x] 結合テストで確認する契約を `.tmp/implement-from-docs/contract-matrix/integration/` に作成する。
- [x] 各契約行を `未処理` として初期化する。
- [x] 対象外契約がある場合は、対象外理由を記録する。
  - `.issue/compose-service-name-mismatch.md` はユーザ合意により今回対象外。

## 実装・単体テスト

### API縦断MVP

- [x] Red: 事前条件、事後条件、不変条件、正常系、異常系、境界値の失敗テストを書く。
- [x] Red: 失敗理由が未実装または設計上の未充足であることを確認する。
- [x] Green: 失敗テストを通す最小実装を行う。
- [x] Green: 対象単体テストを再実行する。
- [x] Refactor: 責務、命名、型、重複、コメント、例外変換、ログ、fixtureを整理する。
- [x] Refactor: 対象範囲の静的テストを実行する。
- [x] 単体テスト用の契約マトリクスを更新する。

### 固定中間メッセージ追加

- [x] Red: 初回生成前、生成完了後、検証開始前、検証合格後、再生成前の固定中間メッセージ契約を単体テストと結合テストへ追加する。
- [x] Green: `ExecuteChatRunUseCase` で固定中間メッセージをDB保存し、SSE `message` として即時配信する。
- [x] Green: `作業を開始します。` が再生成時に重複配信されないことを確認する。
- [x] Green: `command_execution` を中間メッセージとして表示しない既存契約を維持する。
- [x] Refactor: 対象設計書、契約マトリクス、タスクリストを実装内容と整合させる。
- [x] 対象単体・結合テストを実行する。

### SSE保存済み中間メッセージ補完

- [x] Red: SSE接続前に保存済みの中間メッセージを接続直後に配信する結合テストを追加する。
- [x] Green: SSE接続時に現在状態の直後へ保存済み中間メッセージを発生順で補完配信する。
- [x] Green: 画面が接続開始時点で保持済みの中間メッセージを重複表示しない単体テストを追加する。
- [x] Refactor: 対象設計書、契約マトリクス、タスクリストを実装内容と整合させる。

### 初期表示競合の解消

- [x] Red: 初期表示の直近チャット取得が新規送信後に遅延完了しても、実行中セッションを上書きしない単体テストを追加する。
- [x] Green: 起動時取得開始時点のストリーム世代と適用時点の世代を照合し、利用者操作後の古い取得結果を破棄する。
- [x] Refactor: 対象設計書、契約マトリクス、タスクリストを実装内容と整合させる。

### スキーマJSONの中間表示制御

- [x] Red: 検証結果JSONと生成結果JSONが後続イベントで中間候補化されても、スキーマJSON全体を中間メッセージとして保存・通知しない単体テストを追加する。
- [x] Red: 既定DI経由の検証結果JSONが履歴詳細の中間メッセージへ保存されない結合テストを追加する。
- [x] Green: 生成結果JSONは最終回答候補として保留し、最終回答ではないと確定した場合だけ `answers[].text` へ変換する。検証結果JSONはCodex由来中間メッセージから除外する。
- [x] Refactor: 対象設計書、契約マトリクス、タスクリストを実装内容と整合させる。

### 最終回答JSONの中間表示抑止

- [x] Red: 生成結果スキーマJSONの `answers[].text` を候補として抽出しつつ、`turn.completed` 直前の最終回答JSONは中間メッセージへ通知しない単体テストを追加する。
- [x] Green: 生成用Codexの `agent_message.text` が生成結果JSONの場合は最終回答候補として保留し、後続の別 `agent_message` で最終回答ではないと確定した場合だけ `answers[].text` をDB保存・SSE配信用の中間メッセージへ変換する。
- [x] Refactor: 対象設計書、契約マトリクス、タスクリストを最終回答JSONの中間表示抑止仕様と整合させる。

### Codex JSONL互換形式の中間メッセージ抽出

- [x] Red: `event_msg` 形式の開始、エージェントメッセージ、完了と、`response_item` 形式のメッセージ、function call、function call outputを解析する単体テストを追加する。
- [x] Red: `response_item.function_call` と `response_item.function_call_output` を `call_id` で対応付け、短いecho進捗出力だけを中間メッセージとして通知する単体テストを追加する。
- [x] Red: `event_msg.task_complete.last_agent_message` を最終出力として `--output-last-message` と照合する単体テストを追加する。
- [x] Green: `JsonlEventParser`、`CodexIntermediateMessageStreamer`、`CodexRunner` を互換JSONL形式に対応させる。
- [x] Refactor: 対象設計書、契約マトリクス、タスクリストを互換JSONL形式と安全なecho進捗抽出仕様に整合させる。

### 参照元PDF読み取り失敗のエラー終端

- [x] Red: 参照元PDFが存在するのに読み取れない場合、参照元検証が再生成指示を返さずシステムエラーを送出する単体テストを追加する。
- [x] Red: 検証フェーズのPDF読み取り失敗でrunが `エラー` 終端し、画面向けメッセージと検証段階トレースログを保存する単体テストを追加する。
- [x] Green: `cryptography` を実行時依存へ追加し、AES暗号化PDFを `pypdf` で読み取れるようにする。
- [x] Green: PDF実体が存在するのに読み取れない場合は `ReferencePdfReadError` として扱い、再生成へ戻さずエラー終端する。
- [x] Refactor: 対象設計書、契約マトリクス、タスクリストをPDF読み取り失敗仕様に整合させる。
- [x] 対象単体テストと静的テストを実行する。

## 結合テスト

### 対象未確定

- [x] 処理設計書の事前条件、事後条件、不変条件、副作用、例外処理、ログ、トランザクション境界を確認する。
- [x] 外部IF、DB、外部サービス、通信・入出力境界、実行基盤を通した契約を確認する。
- [x] fakeまたはmockで確認する契約と、実環境経由で確認する契約を分ける。
- [x] 結合テストを追加または更新する。
- [x] 対象結合テストを実行する。
- [x] 結合テスト用の契約マトリクスを更新する。

## 品質ゲート

- [x] 契約マトリクスに `未処理` が残っていないことを確認する。
- [x] 対象範囲の静的テストをすべて成功させる。
- [x] バックエンド単体・結合テストをカバレッジ付きで実行し、分岐カバレッジ95.00%を確認する。
  - 分岐カバレッジ95.60%を確認。
- [x] フロントエンド単体・結合テストをカバレッジ付きで実行し、方針書の目標を満たすことを確認する。
  - 単体テスト分岐カバレッジ95.36%、結合テスト分岐カバレッジ80.30%を確認。
- [x] バックエンドevidence方針に従って最新の実行ログまたはcoverage summaryを保存する。
  - `docs/04_テスト/02_単体テスト/evidence/backend-unit-coverage.txt`
  - `docs/04_テスト/03_結合テスト/evidence/backend-integration-evidence.txt`
- [x] フロントエンドevidence方針に従って最新の実行ログまたはcoverage summaryを保存する。
  - `docs/04_テスト/02_単体テスト/evidence/frontend-unit-coverage.txt`
  - `docs/04_テスト/03_結合テスト/evidence/frontend-integration-evidence.txt`

## 総合テスト

- [x] `docs/04_テスト/04_総合テスト/` の対象範囲、対象外範囲、実行方法、証跡保存先を確認する。
- [x] 総合テストを今回対象に含めるか、ユーザ確定範囲と照合する。
  - 総合テストは実Codex CLI、PostgreSQL、バックエンド、フロントエンド、Chrome/Playwrightを同時に使う手動・画面確認工程として定義されている。
- [x] 今回対象の場合、総合テストを実行する。
  - Chromeでチャット実行、キャンセル、履歴再表示の全STケースを実施済み。
  - session `readonly/` への共有データソース提示を実装後、実Codex CLIで参照元付き正常完了と参照元ビューア表示が合格した。
- [x] 総合テストの証跡を方針書どおりに保存または更新する。
  - 全STケースの合格スクリーンショットを `docs/04_テスト/04_総合テスト/evidence/` に保存済み。
- [x] 実行できない総合テストがある場合、理由と代替確認を記録する。
  - 実行不能ケースはなし。
- [x] 今回対象外の場合、対象外理由を記録する。
  - 総合テストは今回対象のため該当なし。

## issue と対象外整理

- [x] 新しい仕様、設計、実装の乖離を `.issue/` に記録する。
- [x] 解消済みissueを削除または更新する。
- [x] 今回対象外の未実装、未解消issue、未反映の任意成果物を整理する。
  - `.issue/compose-service-name-mismatch.md` は今回対象外。
  - `.issue/codex-session-readonly-datasource-missing.md` はsession `readonly/` への共有データソース提示を実装し、ST-CHAT-005 / ST-CHAT-009 合格を確認したため削除した。
  - `.issue/codex-intermediate-message-streaming.md` はcodex exec JSONLの逐次解析、生成アダプタでの中間判定、DB保存/SSE即時配信を実装したため削除した。
  - `.issue/mvp-runtime-execution-incomplete.md` は `execution_deadline_at`、残り秒数算出、二段階キャンセル、REST/SSE/ファイル配信境界トレースを実装したため削除した。
  - `.issue/frontend-test-coverage-missing.md` はフロントエンド単体・結合テストと証跡を整備したため削除した。
  - `.issue/initial-load-race.md` は起動時取得結果の世代照合を実装し、送信後の実行中セッション上書きを解消したため削除した。
  - `.issue/validation-result-json-leaked.md` は最終回答の生成結果JSONと検証結果JSONをCodex由来中間メッセージから除外したため削除した。
- [x] 対象外理由をタスクリストと最終報告に残す。

## 最終報告

- [x] 実装済み範囲を整理する。
- [x] 今回対象外を整理する。
- [x] 未解消issueを整理する。
- [x] 実行したテスト、実行できなかったテスト、証跡を整理する。
- [x] タスクリストに未チェック項目が残っていないことを確認する。
