# ディレクトリ構成

## 1. 文書の目的

本書は、D-Conciergeの内部設計におけるsource-controlledな管理対象ディレクトリを、ルートから末端まで定義することを目的とする。

本書では、`src/` と `codex/` 配下の実装コード、Codex設定資産、テストコード、フロントエンド単体確認用モックを対象にする。生成物、依存ライブラリ、セッション作業領域、データソース実体、トレースログ実体は対象外とする。

## 2. 前提

- 本書に記載するのは `src/` と `codex/` 配下のsource-controlledな管理対象ディレクトリのみとする。
- `src/frontend/node_modules/`、`src/frontend/dist/`、`src/frontend/tsconfig.tsbuildinfo` は管理対象外とする。
- `codex/data_source/`、`codex/sessions/`、`codex/sessions_validator/`、`codex/saved_artifacts/` は実行時または用途別データ配置であり、source-controlledな管理対象外とする。
- `src/frontend/backend_mock/` はフロントエンド単体確認用の開発支援資産として管理するが、アプリ本体のクラス・モジュール設計対象から除外する。

## 3. 管理対象ディレクトリツリー

```text
.
├── codex/                              # 用途別Codex実行へ渡す設定、指示、出力契約
│   ├── .codex/                         # 生成用Codexホームディレクトリ構成
│   │   └── skills/                     # 生成用Codexが利用するSkillsの格納先
│   │       └── custom/                 # 生成用Codexが利用する用途別カスタムSkills
│   ├── .codex_validator/               # 検証用Codexホームディレクトリ構成
│   │   └── skills/                     # 検証用Codexが利用するSkillsの格納先
│   │       └── custom/                 # 検証用Codexが利用する用途別カスタムSkills
│   └── output_json_schema/             # 生成用回答スキーマと検証用結果スキーマ
└── src/                                # 実装コード
    ├── backend/                        # FastAPIバックエンド
    │   ├── app/                        # FastAPIアプリの組み立て
    │   │   ├── router/                 # REST/SSEエンドポイント登録処理
    │   │   └── static/                 # 静的配信設定
    │   ├── presentation/               # HTTP/SSE境界の実装
    │   │   ├── errors/                 # 例外からHTTP応答への変換
    │   │   ├── rest/                   # 機能別RESTエンドポイント、認証依存関係、trace_id受け渡し
    │   │   ├── schemas/                # REST API用Pydanticスキーマ
    │   │   └── sse/                    # SSE接続、イベント配信、送信用payload生成
    │   ├── application/                # ユースケース層
    │   │   ├── account/                # 認証、アカウント登録、ログイン、設定変更、アカウント削除ユースケース
    │   │   ├── artifacts/              # Codex成果物保存・配信ユースケース
    │   │   ├── chat/                   # 新規チャット開始、継続指示、詳細取得、削除受付、物理削除ユースケース
    │   │   ├── execution/              # チャット実行処理、キャンセル、タイムアウト制御
    │   │   ├── history/                # 履歴一覧、履歴再表示、タイトル生成
    │   │   ├── ports/                  # 外部副作用とバックグラウンド登録への抽象IF
    │   │   │   ├── codex/              # 生成、検証、キャンセル、Codex作業領域解決・削除のIF
    │   │   │   ├── database/           # アカウント/チャット永続化IF、削除対象取得IF、Repository戻り値DTO
    │   │   │   ├── filesystem/         # 成果物ファイル保存・読込・削除IF
    │   │   │   ├── runtime/            # 時刻取得、ID発番、受付済みrun・削除ジョブのバックグラウンド登録IF
    │   │   │   ├── security/           # パスワードハッシュとセッショントークン発行・照合用IF
    │   │   │   └── trace_log/          # トレースログ出力IFとログDTO
    │   │   ├── references/             # 表示用参照元メタ情報生成と参照元取得ユースケース
    │   │   └── validation/             # 固定検証処理、Codex検証処理、再生成制御、再生成指示文生成
    │   ├── domain/                     # 副作用を持たない業務判断
    │   │   ├── account/                # ユーザID、ユーザ名、パスワード、ユーザ状態、ログインセッションのルール
    │   │   ├── answer/                 # 回答の不変条件と採用済み回答のルール
    │   │   ├── artifacts/              # Codex成果物の値と不変条件
    │   │   ├── chat/                   # チャット、ユーザ指示、セッション、タイトルのルール
    │   │   ├── execution/              # 実行状態と状態遷移判断
    │   │   ├── references/             # PDF参照元、locator、表示ラベルのルール
    │   │   ├── shared/                 # ドメイン横断の値
    │   │   │   ├── errors/             # ドメイン共通エラー
    │   │   │   ├── ids/                # ドメインID値
    │   │   │   └── time/               # ドメイン日時値
    │   │   └── validation/             # 検証結果と再試行判断
    │   ├── infrastructure/             # 副作用の実装
    │   │   ├── codex/                  # Codex Docker実行、resume、JSONL解析、キャンセル制御、セッション作業領域準備・削除
    │   │   ├── config/                 # 設定読込と設定検証
    │   │   ├── database/               # DB関連実装
    │   │   │   ├── migrations/         # Alembicマイグレーション
    │   │   │   ├── models/             # SQLAlchemy ORMモデル
    │   │   │   ├── repositories/       # Repository実装
    │   │   │   └── session/            # DB接続、セッション、トランザクション処理
    │   │   ├── filesystem/             # ファイル入出力
    │   │   │   ├── file_artifact_store.py   # Codex成果物保存、配信、保存済み成果物削除
    │   │   │   ├── file_reference_store.py  # 参照元ファイル取得
    │   │   │   └── path_security.py         # パス正規化、許可範囲判定、トラバーサル拒否
    │   │   ├── runtime/                # 本番の時刻取得、ID発番、受付済みrun・削除ジョブのバックグラウンド登録
    │   │   ├── security/               # パスワードハッシュ、セッショントークン発行、照合用ハッシュ生成
    │   │   └── trace_log/              # 異常系トレースログYAML出力
    │   ├── shared/                     # 複数レイヤを接続する横断契約、利用者向け表示メッセージ
    │   │   ├── errors/                 # エラー分類と共通基底例外
    │   │   └── tracing/                # trace_idの値と受け渡し型
    │   └── tests/                      # バックエンドテストコード
    │       ├── integration/            # バックエンドの結合テストコード
    │       ├── support/                # テスト用Fake、固定ID、固定時刻など
    │       └── unit/                   # バックエンドの単体テストコード
    └── frontend/                       # React/Viteフロントエンド
        ├── backend_mock/               # フロントエンド単体確認用モック
        │   ├── assets/                 # モックAPIが返す固定ファイル
        │   │   ├── artifacts/          # モック用Codex成果物
        │   │   └── references/         # モック用参照元PDF
        │   ├── data/                   # モック用app-config、履歴、SSEイベント固定データ
        │   └── server/                 # Vite middleware、疑似REST/SSE、モック状態管理
        ├── src/                        # 利用者向けSPA本体
        │   ├── app/                    # ReactアプリのルートとProvider登録
        │   ├── assets/                 # ロゴなどビルド対象の静的アセット
        │   ├── components/             # 複数機能で再利用する画面部品
        │   │   ├── layout/             # AppShell、Sidebar、TopMenuなど全体レイアウト
        │   │   └── ui/                 # shadcn/ui由来の汎用UIラッパー
        │   ├── features/               # 機能別のUI、API境界、モデル、純粋ロジック
        │   │   ├── account/            # ログイン、アカウント登録、認証状態確認、アカウントAPI境界
        │   │   │   ├── api/            # `/api/auth/...`、`/api/account...` 通信とAPIエラー変換
        │   │   │   ├── components/     # ログイン画面、登録画面、認証フォーム共通部品
        │   │   │   ├── lib/            # フォームエラー表示用の変換補助
        │   │   │   └── model/          # アカウント関連型
        │   │   ├── answer-rendering/   # 回答表示機能
        │   │   │   └── components/     # Markdown、Mermaid、画像、HTMLの表示コンポーネント
        │   │   ├── chat/               # チャット機能
        │   │   │   ├── api/            # `/api/...` 通信、SSE購読、削除要求、API応答変換
        │   │   │   ├── components/     # 開始画面、入力欄、スレッド、履歴、削除メニュー、中間メッセージUI
        │   │   │   ├── lib/            # 回答表示演出など純粋ロジック
        │   │   │   └── model/          # チャット関連型
        │   │   ├── reference-viewer/   # 参照元表示機能
        │   │   │   ├── components/     # 参照元リンクとビューアダイアログ
        │   │   │   ├── lib/            # ページ範囲表示など純粋ロジック
        │   │   │   ├── model/          # PDF参照元メタ情報型
        │   │   │   └── viewers/        # PDF.jsビューア
        │   │   └── settings/           # 設定ダイアログとアカウント設定操作
        │   │       └── components/     # 設定ダイアログ、設定枠、アカウント設定一覧、変更フォーム、確認ダイアログ
        │   ├── lib/                    # フロントエンド共通ユーティリティ
        │   ├── pages/                  # 画面単位コンテナ
        │   │   └── chat/               # 開始画面とチャット画面を統合するページ
        │   └── styles/                 # グローバルテーマ、body初期設定、外部ライブラリ生成DOM向けCSS
        └── tests/                      # フロントエンドテストコード
            ├── integration/            # フロントエンドの結合テストコード
            ├── support/                # fetch/EventSourceモック、render helper、fixture
            └── unit/                   # フロントエンドの単体テストコード
```

## 4. 命名と所有ルール

- バックエンドの実装ディレクトリ名は英小文字とアンダースコアを使用する。
- バックエンドの副作用実装は `infrastructure/` 配下へ置き、`domain/` から直接参照しない。
- バックエンドの抽象IFは `application/ports/` へ置く。
- チャット削除関連のユースケース、port、Dispatcher、ファイル削除実装は既存レイヤ配下へ配置し、新規トップレベルディレクトリを作らない。
- アカウント関連ユースケースは `application/account/` へ置き、入力規則とユーザ状態判断は `domain/account/` へ置く。
- パスワードハッシュとログインセッショントークンの発行・照合用ハッシュ生成は `application/ports/security/` で抽象化し、具象実装は `infrastructure/security/` へ置く。
- アカウント削除Dispatcherはチャット削除Dispatcherと同じ `application/ports/runtime/` と `infrastructure/runtime/` の境界に置き、登録集合はチャット削除と分離する。
- `app/` と `main.py` を除くproduction codeは層境界に沿ってimportし、具象実装の組み立てはcomposition rootで行う。
- `presentation/` はAPI/SSE契約への変換に必要な `domain/` のEnumや値オブジェクトを参照してよい。ただし状態遷移、採用可否、検証判定などの業務判断は持たない。
- バックエンドのテストコードは `src/backend/tests/` 配下へ置く。
- フロントエンドの機能固有コードは `src/frontend/src/features/` 配下へ置く。
- ログイン、登録、認証状態確認、アカウントAPIクライアントは `features/account/` に置き、設定ダイアログは将来の設定項目追加を想定して `features/settings/` に置く。
- フロントエンドのテストコードは `src/frontend/tests/` 配下へ置く。
- `src/frontend/src/styles/globals.css` には、Tailwind/shadcnテーマ、body初期設定、外部ライブラリ生成DOM向けスタイルだけを置く。
- `src/frontend/backend_mock/` は `/api/...` 境界を通じたフロントエンド単体確認用資産として維持し、UIコンポーネントから直接参照しない。
