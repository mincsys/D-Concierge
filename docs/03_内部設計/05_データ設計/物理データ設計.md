# 物理データ設計

## 1. 文書の目的

本書は、D-Conciergeで使用する永続ストアの物理配置、項目、制約、索引を定義することを目的とする。

## 2. 前提

- 関連文書: `docs/02_外部設計/05_論理データ設計/論理データ設計.md`、`docs/03_内部設計/04_処理設計/処理一覧.md`、`docs/03_内部設計/06_共通設計/共通設計.md`
- RDBMSはPostgreSQLを使用する。
- テーブル定義とマイグレーションは `src/backend/infrastructure/database/migrations/` で管理する。
- ORMモデルは `src/backend/infrastructure/database/models/base.py`、`user.py`、`chat.py`、`answer.py` に置き、application層へSQLAlchemyモデルを返さない。
- DBには論理データと保存済みファイルのメタ情報を保持し、参照元PDF実体、Codex成果物実体、トレースログ実体はファイルとして保持する。
- Codex出力スキーマの参照ページは `start_page` / `end_page` だが、DBへ保存するlocatorは正規化後の `page_start` / `page_end` とする。
- Codex出力スキーマの参照元pathは `readonly/` から始まるCodex作業領域上の相対パスだが、DBへ保存するlocatorの `path` は `readonly/` を除いた共有データソースルートからの相対パスとする。

## 3. ストレージ一覧

| ストレージ名 | 種別 | 用途 |
| --- | --- | --- |
| `users` | PostgreSQLテーブル | ログインユーザ、ユーザ名、認証情報、利用状態を保持する。 |
| `login_sessions` | PostgreSQLテーブル | ブラウザごとのログインセッションを保持する。 |
| `chats` | PostgreSQLテーブル | チャット単位、チャット状態、履歴タイトル、Codex側resume用IDを保持する。 |
| `chat_runs` | PostgreSQLテーブル | ユーザ指示1回ごとの実行状態、開始/終了日時、利用者向けメッセージを保持する。 |
| `user_instructions` | PostgreSQLテーブル | runに対応する利用者指示本文を保持する。 |
| `intermediate_messages` | PostgreSQLテーブル | 履歴再表示用の中間メッセージを保持する。 |
| `answer_blocks` | PostgreSQLテーブル | 完了runに紐づき、Codex出力の `answers` 単位で検証済み回答ブロック本文と表示順を保持する。 |
| `references` | PostgreSQLテーブル | 回答ブロックに紐付くPDF参照元メタ情報とlocatorを保持する。 |
| `artifacts` | PostgreSQLテーブル | 回答ブロックに紐付く保存済みCodex成果物メタ情報を保持する。 |
| `generator.saved_artifacts_dir` | ファイル領域 | 採用済みCodex成果物の実体を保持する。 |
| `generator.workdir` | ファイル領域 | 生成用Codexのセッション作業領域を保持する。 |
| `validator.workdir` | ファイル領域 | 検証用Codexのセッション作業領域を保持する。 |
| `trace_log.dir` | ファイル領域 | 異常系トレースログYAMLを保持する。 |

## 4. 物理定義

### 4.1. `users`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `users` |
| 用途 | ログインユーザ、ユーザ名、認証情報、利用状態を保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `varchar(30)` | 否 | PK | ユーザID。 |
| `user_name` | `varchar(30)` | 否 |  | 画面表示名。 |
| `password_hash` | `text` | 否 |  | パスワード検証に使用するハッシュ値。 |
| `user_state` | `varchar(20)` | 否 | DEFAULT `active`、CHECK | `active`、`deleting`。 |
| `created_at` | `timestamptz` | 否 |  | 作成日時。 |
| `updated_at` | `timestamptz` | 否 |  | 更新日時。 |

### 4.2. `login_sessions`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `login_sessions` |
| 用途 | ブラウザごとのログインセッションを保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `bigint` | 否 | PK、IDENTITY | ログインセッション行を識別する内部連番ID。 |
| `token_hash` | `text` | 否 | UNIQUE | Cookieへ発行したログインセッショントークンの照合用ハッシュ値。 |
| `user_id` | `varchar(30)` | 否 | FK -> `users.id` | セッションを所有するユーザID。 |
| `expires_at` | `timestamptz` | 否 |  | セッション有効期限。 |
| `created_at` | `timestamptz` | 否 |  | 作成日時。 |
| `updated_at` | `timestamptz` | 否 |  | 更新日時。 |

### 4.3. `chats`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `chats` |
| 用途 | 一連の指示応答をまとめるチャット単位を保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | 否 | PK | チャットID。 |
| `user_id` | `varchar(30)` | 否 | FK -> `users.id` | 所有するユーザID。 |
| `session_id` | `uuid` | 否 | UNIQUE | D-ConciergeのCodex作業領域を決める内部ID。Codex側resume用IDとは別管理とする。 |
| `chat_state` | `varchar(20)` | 否 | DEFAULT `active`、CHECK | `active`、`deleting`。チャット単位のライフサイクル状態。 |
| `title` | `varchar(50)` | 否 |  | 履歴一覧表示用タイトル。 |
| `generation_conversation_id` | `varchar(255)` | 可 |  | 生成用Codex側のresume用ID。 |
| `validation_conversation_id` | `varchar(255)` | 可 |  | 検証用Codex側のresume用ID。 |
| `updated_at` | `timestamptz` | 否 |  | 履歴一覧の並び順に使う最終更新日時。 |

### 4.4. `chat_runs`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `chat_runs` |
| 用途 | ユーザ指示1回ごとの実行状態を保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | 否 | PK | チャット実行処理ID。 |
| `chat_id` | `uuid` | 否 | FK -> `chats.id` | 所属チャットID。 |
| `state` | `varchar(20)` | 否 | CHECK | `accepted`、`running`、`validating`、`cancel_requested`、`canceled`、`completed`、`error`、`timed_out`。 |
| `started_at` | `timestamptz` | 否 |  | 受付日時。 |
| `execution_deadline_at` | `timestamptz` | 可 |  | `running` へ遷移した時点で計算する、生成から検証完了までの全体deadline。 |
| `ended_at` | `timestamptz` | 可 |  | 終端状態確定日時。 |
| `user_message` | `text` | 可 |  | 利用者向け状態/エラーメッセージ。 |

### 4.5. `user_instructions`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `user_instructions` |
| 用途 | runに対応する利用者指示本文を保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | 否 | PK | ユーザ指示ID。 |
| `run_id` | `uuid` | 否 | UNIQUE、FK -> `chat_runs.id` | 対象run ID。 |
| `body` | `text` | 否 | CHECK `length(trim(body)) > 0` | 利用者指示本文。 |

### 4.6. `intermediate_messages`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `intermediate_messages` |
| 用途 | 履歴再表示用の中間メッセージを保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | 否 | PK | 中間メッセージID。 |
| `run_id` | `uuid` | 否 | FK -> `chat_runs.id` | 対象run ID。 |
| `body` | `text` | 否 |  | 画面表示用に整形・マスク済みの本文。 |
| `created_at` | `timestamptz` | 否 |  | 作成日時。run内表示順の主条件として使う。 |

### 4.7. `answer_blocks`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `answer_blocks` |
| 用途 | run内の表示ブロックをCodex出力の `answers` 配列順で保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | 否 | PK | 回答ブロックID。 |
| `run_id` | `uuid` | 否 | FK -> `chat_runs.id` | 対象run ID。 |
| `position` | `integer` | 否 | CHECK `position > 0`、UNIQUE(`run_id`, `position`) | run内の表示順。1始まりで保持する。 |
| `markdown` | `text` | 否 |  | 検証済み回答ブロック本文。 |

### 4.8. `references`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `references` |
| 用途 | 回答ブロックに紐付くPDF参照元メタ情報を保持する。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | 否 | PK | 参照元ID。 |
| `answer_block_id` | `uuid` | 否 | FK -> `answer_blocks.id` | 対象回答ブロックID。 |
| `position` | `integer` | 否 | CHECK `position > 0`、UNIQUE(`answer_block_id`, `position`) | 回答ブロック内の表示順。1始まりで保持する。 |
| `source_type` | `varchar(20)` | 否 | CHECK `source_type = 'pdf'` | 本システムではPDFのみ。 |
| `label` | `varchar(255)` | 否 |  | 画面表示ラベル。 |
| `locator` | `jsonb` | 否 | CHECK | 共有データソースルートからの相対 `path`、`page_start`、`page_end` を保持する。 |

### 4.9. `artifacts`

| 項目 | 内容 |
| --- | --- |
| 物理名 | `artifacts` |
| 用途 | 採用済み回答ブロックが参照する保存済みCodex成果物メタ情報を保持する。DB上の表示順は保持しない。 |

| カラム名 | 型 | NULL可否 | 制約 | 説明 |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | 否 | PK | Codex成果物ID。 |
| `answer_block_id` | `uuid` | 否 | FK -> `answer_blocks.id` | 対象回答ブロックID。 |
| `mime_type` | `varchar(100)` | 否 |  | 配信時Content-Type。 |
| `storage_path` | `text` | 否 | UNIQUE | 保存済み成果物領域からの相対参照。`<user-id>/<run_id>/<artifact_id>.<拡張子>` 形式。 |
| `created_at` | `timestamptz` | 否 |  | 作成日時。 |

### 4.10. 保存済みCodex成果物領域

| 項目 | 内容 |
| --- | --- |
| 物理名 | `generator.saved_artifacts_dir` |
| 用途 | 採用済み回答が使用する成果物実体を保存する。 |
| 配置規則 | `<generator.saved_artifacts_dir>/<user-id>/<run_id>/<artifact_id>.<拡張子>` |
| DB参照 | `artifacts.storage_path` には `<user-id>/<run_id>/<artifact_id>.<拡張子>` 形式の相対参照だけを保持する。 |

### 4.11. 生成用Codexセッション領域

| 項目 | 内容 |
| --- | --- |
| 物理名 | `generator.workdir` |
| 用途 | 生成用codex execのresume可能な作業領域を保持する。 |
| 配置規則 | `<generator.workdir>/<user-id>/<session-id>/readonly/`、`tmp/`、`artifacts/`。`readonly/` は共有データソースディレクトリへのシンボリックリンクとして作成する。 |
| DB参照 | `chats.session_id` と `chats.generation_conversation_id` で作業領域とCodex側resume用IDを関連付ける。 |

### 4.12. 検証用Codexセッション領域

| 項目 | 内容 |
| --- | --- |
| 物理名 | `validator.workdir` |
| 用途 | 検証用codex execのresume可能な作業領域を保持する。 |
| 配置規則 | `<validator.workdir>/<user-id>/<session-id>/readonly/`、`tmp/`。`readonly/` は共有データソースディレクトリへのシンボリックリンクとして作成する。検証対象回答が成果物リンクを含む場合だけ、生成用セッション領域の `artifacts/` を同名のシンボリックリンクとして提示する。 |
| DB参照 | `chats.session_id` と `chats.validation_conversation_id` で作業領域とCodex側resume用IDを関連付ける。 |

### 4.13. トレースログ領域

| 項目 | 内容 |
| --- | --- |
| 物理名 | `trace_log.dir` |
| 用途 | 異常系トレースログYAMLを保存する。 |
| 配置規則 | `<trace_log.dir>/<yyyy-MM-dd>/<HH-mm-ss>_<microseconds>_<event_name>.yaml`。日時部分は `app.timezone` 基準。同名ファイルが存在する場合は末尾へ連番を付ける。 |
| 保持規則 | `trace_log.retention_days` に従い、`app.timezone` 基準で保存期間を過ぎた `<yyyy-MM-dd>` 日付ディレクトリをアプリケーション起動時に削除する。`trace_log.max_files_per_day` に従い、アプリケーション起動ごとの同日保存件数を制限する。 |
| DB参照 | DBテーブルへは保持しない。trace_idでAPI、ユースケース、ログを関連付ける。 |

## 5. 制約と索引

| 対象 | 種別 | 内容 |
| --- | --- | --- |
| `users.id` | CHECK | 1文字以上30文字以内で、半角英数字、`_`、`-` のみを許可し、先頭と末尾は半角英数字とする。 |
| `users.user_state` | CHECK | `active`、`deleting` だけを許可する。 |
| `login_sessions.token_hash` | UNIQUE | Cookieへ発行したログインセッショントークンの照合用ハッシュ値を一意にする。 |
| `login_sessions.user_id` | FK | `users.id` を参照し、アカウント削除時は対象ユーザのセッションを削除する。 |
| `login_sessions(user_id, expires_at)` | INDEX | ユーザ単位のセッション削除と期限確認に使用する。 |
| `login_sessions(expires_at)` | INDEX | アプリケーション起動時の期限切れセッション削除に使用する。 |
| `chats.user_id` | FK | `users.id` を参照し、アカウント削除時は対象ユーザのチャットを削除する。 |
| `chats.session_id` | UNIQUE | Codex作業領域をチャット単位で一意にする。 |
| `chats.chat_state` | CHECK | `active`、`deleting` だけを許可する。 |
| `chats(user_id, chat_state, updated_at desc)` | INDEX | 履歴一覧を利用者別、チャット状態別、更新日時降順で取得する。履歴一覧は `chat_state = 'active'` を対象にする。 |
| `chat_runs.chat_id` | FK | `chats.id` を参照し、チャット削除時は関連runを削除する。 |
| `chat_runs.state` | CHECK | 定義済み8状態だけを許可する。 |
| `chat_runs(chat_id, started_at, id)` | INDEX | チャット詳細でrunを開始日時とIDの安定順に取得する。 |
| `chat_runs(chat_id, state)` | INDEX | 未完了run確認に使用する。 |
| `chat_runs_one_unfinished_per_chat` | 部分UNIQUE INDEX | `chat_id` を対象に、`state IN ('accepted','running','validating','cancel_requested')` の行を同一チャットで1件だけ許可する。 |
| `user_instructions.run_id` | UNIQUE/FK | runとユーザ指示を1対1にする。 |
| `intermediate_messages(run_id, created_at, id)` | INDEX | run内の中間メッセージを作成日時とIDの安定順で取得する。 |
| `answer_blocks.run_id` | FK | `chat_runs.id` を参照し、run削除時は回答ブロックを削除する。 |
| `answer_blocks(run_id, position)` | UNIQUE | 回答ブロックのrun内表示順を一意にし、表示順で取得する。 |
| `references.answer_block_id` | FK | `answer_blocks.id` を参照し、回答ブロック削除時は参照元を削除する。 |
| `references(answer_block_id, position)` | UNIQUE | 参照元の回答ブロック内表示順を一意にし、表示順で取得する。 |
| `references.locator` | CHECK | `path` が文字列、`page_start` と `page_end` が正の整数、`page_start <= page_end` であることを検証する。`path` はアプリケーション層で共有データソースルートからの相対PDFパスへ正規化済みであることを前提とする。 |
| `artifacts.answer_block_id` | FK | `answer_blocks.id` を参照し、回答ブロック削除時は成果物メタ情報を削除する。 |
| `artifacts.storage_path` | UNIQUE | 同一保存先参照の重複登録を防ぐ。 |

## 6. 保持期間と整合性

- チャット履歴、ユーザ指示、実行状態、中間メッセージ、回答ブロック、参照元、Codex成果物メタ情報は削除操作が行われない限り保持する。
- 保存済みCodex成果物実体は、対応する `artifacts` レコードが存在し、削除操作が行われない限り保持する。
- ログインセッションは、ログアウト、アカウント削除、または有効期限切れ削除が行われるまで保持する。
- 削除受付後のチャットは `chat_state = 'deleting'` のまま保持し、履歴一覧、履歴詳細、継続指示、参照元取得、成果物取得の通常対象から除外する。
- チャット物理削除処理は、生成用・検証用作業領域と保存済みCodex成果物実体の削除が完了した後、対象チャットに紐づくDBデータをカスケード削除する。
- 物理削除失敗時は `chat_state = 'deleting'` のまま維持し、起動時再登録で再試行できる状態にする。
- アカウント削除受付後のユーザは `user_state = 'deleting'` のまま保持し、認証、ログイン、ユーザ名変更、パスワード変更、チャット通常操作の対象外とする。
- アカウント物理削除処理は、生成用・検証用のユーザ単位作業領域と保存済みCodex成果物のユーザ単位ディレクトリの削除が完了した後、対象ユーザに紐づくDBデータを削除する。
- アカウント物理削除失敗時は `user_state = 'deleting'` のまま維持し、起動時再登録で再試行できる状態にする。
- セッション内 `artifacts/` は採用前の一時領域であり、キャンセル、エラー、タイムアウト、検証失敗で終端したrunの成果物候補は保存済み成果物へコピーしない。
- トレースログはDBの参照整合性対象には含めない。標準では90日保持し、保存期間を過ぎた日付ディレクトリを起動時に削除する。同日最大保存件数はアプリケーション起動後の書き込み件数だけで判定し、標準では1000件とする。
- チャット詳細再表示ではDBを正とし、SSE未受信イベントがあってもDB保存済み状態から復元する。
- DB commit後にSSE配信へ失敗した場合、DB状態を巻き戻さない。
- ファイル保存とDBメタ情報保存の間で失敗した場合は、処理設計に従い未参照ファイルを後続の保守対象とし、回答採用を完了扱いにしない。
