# 回答検証と実行制御・エラー処理

## 目的

本メモは、中間メッセージ領域の折りたたみ表示仕様を整理する。

## 中間メッセージ領域の折りたたみ

最終回答が表示された後、中間メッセージ領域は折りたたみ、次のような見出しだけを表示する。

```text
Thought for 16s
```

`16s` は、ユーザ指示送信から最終回答の検証完了までの経過時間を表す。

利用者が `Thought for <秒数>` をクリックすると、中間メッセージ領域を再展開し、生成用codex execと検証用codex execの中間メッセージを時系列で表示する。
