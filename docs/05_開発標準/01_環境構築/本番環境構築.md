# 本番環境構築

## 1. 文書の目的

本書は、D-Conciergeの本番環境を構築し、社内LANで実運用できる状態にするための環境構成、構築手順、起動確認、監視、ログ、バックアップおよび障害時対応の入口を定義することを目的とする。

本書では、本番環境の構築パターンとしてDocker Compose版、Linuxアプリ直接配置版、Windowsアプリ直接配置版を定義する。

## 2. 前提

- 本番環境は社内LANでの小規模利用を対象とする。
- 本番環境は単一ホスト構成とし、Web画面、REST API、SSE、Codex実行コンテナ、PostgreSQLを同一ホスト上で動作させる。
- 利用者端末からPostgreSQLへ直接接続しない。
- `config.example.yaml` はGit管理対象の設定ひな形とし、本番環境ではこれをコピーして `config.yaml` を作成する。
- `config.yaml` は本番環境固有値と秘密値を含むためGit管理対象外とする。
- Codex実行コンテナで使用する認証情報、用途別Codexホーム、作業領域は本番ホスト上で事前に準備し、Git管理対象外とする。
- アプリケーションプロセスは、Codex実行コンテナを起動できるDocker実行権限を持つ。
- DB migrationはAlembicで実行する。
- バックアップは任意とし、実施有無、取得頻度、保存先、保存期間は運用者が決定する。
- 本番環境で使用するDBパスワード、Codex認証情報、サーバアカウント情報、個人情報は本書に記載しない。

## 3. 本番環境構成

### 3.1. 構成パターン

| パターン | 用途 | 実行基盤 | 起動管理 |
| --- | --- | --- | --- |
| Docker Compose版 | 社内配布の標準構成 | `infra/Dockerfile`、`infra/compose.yml` | Docker Compose |
| Linuxアプリ直接配置版 | Linux単一ホストへアプリケーションを直接配置する構成 | ホスト上のPython、Node.js、PostgreSQL、Docker | systemd |
| Windowsアプリ直接配置版 | Windows単一ホストへアプリケーションを直接配置する構成 | ホスト上のPython、Node.js、PostgreSQL、Docker | タスクスケジューラ |

### 3.2. 構成要素

| 構成要素 | 用途 | 配置先 | 備考 |
| --- | --- | --- | --- |
| フロントエンド静的ファイル | Web画面表示 | アプリケーション配信領域 | `src/frontend` のビルド成果物 |
| バックエンド | REST、SSE、静的配信、実行制御 | アプリケーションホスト | FastAPI、Uvicornで起動する |
| PostgreSQL | 履歴、実行状態、回答、参照元、成果物メタ情報の保存 | アプリケーションホスト | 利用者端末へ直接公開しない |
| 生成用Codex実行コンテナ | 回答生成 | Docker | 生成用Codexホームと生成用作業領域をマウントして実行する |
| 検証用Codex実行コンテナ | 回答検証 | Docker | 検証用Codexホームと検証用作業領域をマウントして実行する |
| 共有データソース | Codexの検索・分析対象 | `codex/readonly` | 利用者は直接参照しない |
| Codex作業領域 | 生成用・検証用Codex実行コンテナの作業領域 | `codex/sessions`、`codex/sessions_validator` | ユーザIDとセッションIDで分離する |
| 保存済みCodex成果物 | 検証済み回答が参照する成果物本体 | `codex/saved_artifacts` | API経由で配信する |
| トレースログ | 異常系障害調査用YAMLログ | `logs/trace` | 利用者画面へ表示しない |
| アプリ共通タイムゾーン | 運用者向け日時の基準 | `app.timezone` | IANA timezone名で指定し、標準は `Asia/Tokyo` とする |

## 4. 必要なツール・アカウント

| 区分 | ツール・アカウント | 必要な権限 | 用途 |
| --- | --- | --- | --- |
| 共通 | Git | リポジトリ取得権限 | 本番配置対象の取得 |
| 共通 | PostgreSQL管理権限 | DB作成、migration、初期データ投入 | 永続化DBの構築 |
| 共通 | psql | DB接続権限 | DB確認 |
| 共通 | Docker | コンテナ起動権限 | Codex実行コンテナの起動 |
| 共通 | Codex実行用Dockerイメージ | `codex-python-runner:latest` の利用権限 | 回答生成・回答検証 |
| Docker Compose版 | Docker | コンテナ起動権限 | アプリケーション、PostgreSQL、Codex実行コンテナの起動 |
| Docker Compose版 | Docker Compose | Compose実行権限 | `infra/compose.yml` の実行 |
| Linuxアプリ直接配置版 | Python 3.14、uv | アプリ配置先での実行権限 | バックエンド実行、migration |
| Linuxアプリ直接配置版 | Node.js 24、npm | アプリ配置先での実行権限 | フロントエンドビルド |
| Linuxアプリ直接配置版 | systemd管理権限 | サービス登録、起動、停止 | バックエンド常駐管理 |
| Windowsアプリ直接配置版 | Python 3.14、uv | アプリ配置先での実行権限 | バックエンド実行、migration |
| Windowsアプリ直接配置版 | Node.js 24、npm | アプリ配置先での実行権限 | フロントエンドビルド |
| Windowsアプリ直接配置版 | タスクスケジューラ管理権限 | タスク登録、起動、停止 | バックエンド常駐管理 |

## 5. 環境変数・機密情報

| 項目 | 管理方法 | 用途 | 備考 |
| --- | --- | --- | --- |
| `config.example.yaml` | Git管理 | 本番設定ひな形 | 秘密値を含めない |
| `config.yaml` | 本番ホスト上で管理 | 本番アプリ設定 | Git管理しない |
| `database.url` | `config.yaml` | アプリケーション実行時のDB接続先 | DBパスワードを含む場合はファイル権限で保護する |
| `D_CONCIERGE_DATABASE_URL` | migration実行時の環境変数 | AlembicのDB接続先 | 恒久設定ではなく実行時だけ指定する |
| `POSTGRES_DB` | Docker Compose環境変数 | PostgreSQL DB名 | Docker Compose版で使用する |
| `POSTGRES_USER` | Docker Compose環境変数 | PostgreSQLユーザ | Docker Compose版で使用する |
| `POSTGRES_PASSWORD` | Docker Compose環境変数 | PostgreSQLパスワード | 本番値をGit管理しない |
| `APP_PORT` | Docker Compose環境変数 | アプリケーション公開ポート | 未指定時は `8000` |
| `codex_docker.image` | `config.yaml` | Codex実行コンテナのDockerイメージ | 標準は `codex-python-runner:latest` |
| `codex_docker.workspace_dir` | `config.yaml` | コンテナ内Codex作業ディレクトリ | 標準は `/workspace` |
| `codex_docker.codex_home_dir` | `config.yaml` | コンテナ内Codexホームディレクトリ | 標準は `/home/codex/.codex` |
| `codex_docker.codex_api_key` | `config.yaml` | Codex実行コンテナへ渡す `CODEX_API_KEY` | 空文字を指定できる |
| Codexホーム認証情報 | 本番ホスト上で管理 | Codex実行コンテナの認証 | `codex/.codex`、`codex/.codex_validator` 配下で管理し、Git管理しない |
| `UV_CACHE_DIR` | OS環境変数またはサービス環境変数 | uvキャッシュ保存先 | Linuxでは `/tmp/uv-cache`、Windowsでは `%TEMP%\uv-cache` |

## 6. インフラ準備

### 6.1. Docker Compose版

本番ホストにDockerとDocker Composeを導入し、リポジトリを配置する。

設定ファイルと永続領域を準備する。

```bash
cp config.example.yaml config.yaml
mkdir -p logs/trace codex/readonly codex/.codex codex/.codex_validator codex/saved_artifacts codex/sessions codex/sessions_validator
```

Codex実行用Dockerイメージをビルドする。

```bash
docker build -t codex-python-runner:latest -f infra/codex_docker/Dockerfile infra/codex_docker
```

`config.yaml` の `database.url` は、Docker Compose内部のPostgreSQLへ接続する値へ変更する。Codex Docker設定は本番ホスト上で利用するイメージ名、コンテナ内作業ディレクトリ、コンテナ内Codexホーム、認証値を指定する。

```yaml
database:
  url: "postgresql+psycopg://d_concierge:<本番DBパスワード>@postgres:5432/d_concierge"

codex_docker:
  image: "codex-python-runner:latest"
  workspace_dir: "/workspace"
  codex_home_dir: "/home/codex/.codex"
  codex_api_key: ""
```

Docker Compose実行時には本番DBパスワードを環境変数で指定する。

```bash
export POSTGRES_DB='d_concierge'
export POSTGRES_USER='d_concierge'
export POSTGRES_PASSWORD='<本番DBパスワード>'
export APP_PORT='8000'
```

Docker Compose版では、ホスト側の `codex/` をアプリケーションから参照できる状態にし、バックエンドがDocker上のCodex実行コンテナへ用途別Codexホーム、作業領域、共有データソースをマウントする。運用者は、ホスト側の `codex/.codex` と `codex/.codex_validator` に `AGENTS.md`、Skills、認証情報を配置する。

### 6.2. Linuxアプリ直接配置版

本番ホストにPython 3.14、uv、Node.js 24、npm、PostgreSQL、psql、Dockerを導入し、リポジトリを配置する。

アプリケーション配置先を `/opt/d-concierge` とする。

```bash
id -u d-concierge >/dev/null 2>&1 || sudo useradd --system --home /opt/d-concierge --shell /usr/sbin/nologin d-concierge
sudo mkdir -p /opt/d-concierge
sudo chown -R d-concierge:d-concierge /opt/d-concierge
```

設定ファイルと永続領域を準備する。

```bash
cp config.example.yaml config.yaml
mkdir -p logs/trace codex/readonly codex/.codex codex/.codex_validator codex/saved_artifacts codex/sessions codex/sessions_validator
```

Codex実行用Dockerイメージをビルドする。

```bash
docker build -t codex-python-runner:latest -f infra/codex_docker/Dockerfile infra/codex_docker
```

PostgreSQLに本番DBと本番ユーザを作成し、`config.yaml` の `database.url` を本番DB接続先へ変更する。

### 6.3. Windowsアプリ直接配置版

本番ホストにPython 3.14、uv、Node.js 24、npm、PostgreSQL、psql、Dockerを導入し、リポジトリを配置する。

アプリケーション配置先を `C:\D-Concierge` とする。

```powershell
New-Item -ItemType Directory -Force C:\D-Concierge
```

設定ファイルと永続領域を準備する。

```powershell
Copy-Item config.example.yaml config.yaml
New-Item -ItemType Directory -Force logs\trace
New-Item -ItemType Directory -Force codex\readonly
New-Item -ItemType Directory -Force codex\.codex
New-Item -ItemType Directory -Force codex\.codex_validator
New-Item -ItemType Directory -Force codex\saved_artifacts
New-Item -ItemType Directory -Force codex\sessions
New-Item -ItemType Directory -Force codex\sessions_validator
```

Codex実行用Dockerイメージをビルドする。

```powershell
docker build -t codex-python-runner:latest -f infra/codex_docker/Dockerfile infra/codex_docker
```

PostgreSQLに本番DBと本番ユーザを作成し、`config.yaml` の `database.url` を本番DB接続先へ変更する。

### 6.4. Codex実行資産の準備

生成用Codexホームと検証用Codexホームには、それぞれの用途に応じた `AGENTS.md`、Skills、認証情報を配置する。`codex_docker.codex_api_key` にAPIキーを設定する場合は、Codex実行コンテナへ `CODEX_API_KEY` として渡される。空文字の場合は、Codexホーム配下の認証情報を使用する。

検証用Codexへ生成成果物を提示する処理では、生成用セッションの成果物領域を検証用Codex実行コンテナへ読み取り専用でマウントする。検証時に生成用成果物領域が存在しない場合は、成果物領域のマウントを行わない。

## 7. アプリケーション配置

### 7.1. Docker Compose版

本番イメージをビルドする。

```bash
docker compose -f infra/compose.yml build app
```

### 7.2. Linuxアプリ直接配置版

Python依存関係を同期する。

```bash
UV_CACHE_DIR=/tmp/uv-cache uv sync --python 3.14 --frozen --no-dev
```

フロントエンドをビルドし、バックエンドの静的配信領域へ配置する。

```bash
cd src/frontend
npm ci
npm run build
cd ../..
rm -rf src/backend/app/static/dist
mkdir -p src/backend/app/static
cp -R src/frontend/dist src/backend/app/static/dist
```

### 7.3. Windowsアプリ直接配置版

Python依存関係を同期する。

```powershell
$env:UV_CACHE_DIR = "$env:TEMP\uv-cache"
uv sync --python 3.14 --frozen --no-dev
```

フロントエンドをビルドし、バックエンドの静的配信領域へ配置する。

```powershell
Set-Location src/frontend
npm ci
npm run build
Set-Location ../..
Remove-Item -Recurse -Force src/backend/app/static/dist -ErrorAction SilentlyContinue
New-Item -ItemType Directory -Force src/backend/app/static
Copy-Item -Recurse src/frontend/dist src/backend/app/static/dist
```

## 8. 初期データ・マイグレーション

### 8.1. Docker Compose版

PostgreSQLを起動する。

```bash
docker compose -f infra/compose.yml up -d postgres
```

DB migrationを実行する。

```bash
docker compose -f infra/compose.yml run --rm \
  -e D_CONCIERGE_DATABASE_URL='postgresql+psycopg://d_concierge:<本番DBパスワード>@postgres:5432/d_concierge' \
  app .venv/bin/alembic upgrade head
```

### 8.2. Linuxアプリ直接配置版

DB migrationを実行する。

```bash
D_CONCIERGE_DATABASE_URL='postgresql+psycopg://d_concierge:<本番DBパスワード>@127.0.0.1:5432/d_concierge' \
UV_CACHE_DIR=/tmp/uv-cache \
.venv/bin/alembic upgrade head
```

### 8.3. Windowsアプリ直接配置版

DB migrationを実行する。

```powershell
$env:D_CONCIERGE_DATABASE_URL = "postgresql+psycopg://d_concierge:<本番DBパスワード>@127.0.0.1:5432/d_concierge"
$env:UV_CACHE_DIR = "$env:TEMP\uv-cache"
.\.venv\Scripts\alembic.exe upgrade head
```

## 9. 起動・停止手順

### 9.1. Docker Compose版

起動する。

```bash
docker compose -f infra/compose.yml up -d --build app
```

停止する。

```bash
docker compose -f infra/compose.yml down
```

アプリケーションだけを再起動する。

```bash
docker compose -f infra/compose.yml restart app
```

### 9.2. Linuxアプリ直接配置版

systemdサービスを作成する。

```bash
sudo tee /etc/systemd/system/d-concierge.service >/dev/null <<'UNIT'
[Unit]
Description=D-Concierge
After=network.target postgresql.service

[Service]
Type=simple
User=d-concierge
WorkingDirectory=/opt/d-concierge
Environment=UV_CACHE_DIR=/tmp/uv-cache
ExecStart=/opt/d-concierge/.venv/bin/uvicorn backend.main:app --app-dir src --host 0.0.0.0 --port 8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
UNIT
```

起動する。

```bash
sudo systemctl daemon-reload
sudo systemctl enable d-concierge
sudo systemctl start d-concierge
```

停止する。

```bash
sudo systemctl stop d-concierge
```

再起動する。

```bash
sudo systemctl restart d-concierge
```

### 9.3. Windowsアプリ直接配置版

タスクスケジューラに起動タスクを登録する。作業ディレクトリは `C:\D-Concierge` とする。

```powershell
$Action = New-ScheduledTaskAction `
  -Execute "powershell.exe" `
  -Argument "-NoProfile -ExecutionPolicy Bypass -Command `"Set-Location C:\D-Concierge; `$env:UV_CACHE_DIR=`"`$env:TEMP\uv-cache`"; .\.venv\Scripts\uvicorn.exe backend.main:app --app-dir src --host 0.0.0.0 --port 8000`""
$Trigger = New-ScheduledTaskTrigger -AtStartup
Register-ScheduledTask -TaskName "D-Concierge" -Action $Action -Trigger $Trigger -RunLevel Highest
```

起動する。

```powershell
Start-ScheduledTask -TaskName "D-Concierge"
```

停止する。

```powershell
Stop-ScheduledTask -TaskName "D-Concierge"
```

タスクを削除する。

```powershell
Unregister-ScheduledTask -TaskName "D-Concierge" -Confirm:$false
```

## 10. 動作確認

| 確認項目 | 確認方法 | 期待結果 |
| --- | --- | --- |
| アプリケーション起動 | `http://<本番ホスト>:8000` をChromeで開く | 開始画面またはチャット画面が表示される |
| アプリ設定API | `curl http://<本番ホスト>:8000/api/app-config` | 歓迎メッセージと入力候補だけが返る |
| DB migration | `alembic_version` テーブルを確認する | revision `0001` 以上が記録されている |
| Codex実行用Dockerイメージ | `docker run --rm codex-python-runner:latest codex --version` を実行する | コンテナ内Codexのバージョンが表示される |
| トレースログ領域 | `logs/trace` を確認する | アプリケーション実行ユーザが書き込める |
| 保存済み成果物領域 | `codex/saved_artifacts` を確認する | アプリケーション実行ユーザが書き込める |
| 共有データソース | `codex/readonly` を確認する | Codex実行コンテナへ読み取り専用でマウントできる |

## 11. 監視・ログ・バックアップ

### 11.1. 監視・ログ確認

| パターン | 確認対象 | 確認コマンド |
| --- | --- | --- |
| Docker Compose版 | アプリケーションログ | `docker compose -f infra/compose.yml logs app` |
| Docker Compose版 | PostgreSQLログ | `docker compose -f infra/compose.yml logs postgres` |
| Linuxアプリ直接配置版 | systemdログ | `journalctl -u d-concierge` |
| Windowsアプリ直接配置版 | タスク状態 | `Get-ScheduledTask -TaskName "D-Concierge"` |
| 共通 | トレースログ | `logs/trace/<yyyy-MM-dd>/` 配下のYAMLファイル |
| 共通 | DB容量 | PostgreSQLのDBサイズ |
| 共通 | 成果物容量 | `codex/saved_artifacts` の使用量 |
| 共通 | Codex作業領域容量 | `codex/sessions`、`codex/sessions_validator` の使用量 |
| 共通 | トレースログ容量 | `logs/trace` の使用量 |

容量確認は運用者が定期的に実施する。トレースログは `app.timezone` 基準の日付で、`trace_log.retention_days` の保存期間を過ぎた日付ディレクトリをアプリケーション起動時に削除し、`trace_log.max_files_per_day` の設定に従ってアプリケーション起動ごとの同日保存件数を制限する。チャット履歴、DB、保存済みCodex成果物の自動削除機能は本システムでは提供しない。

### 11.2. 任意バックアップ

バックアップ実施有無、頻度、保存先、保存期間は運用者が決定する。バックアップを行う場合は、PostgreSQL、用途別Codexホーム、Codex作業領域、保存済みCodex成果物、トレースログ、`config.yaml` を対象にする。

Docker Compose版のPostgreSQLバックアップ例:

```bash
mkdir -p backup
docker compose -f infra/compose.yml exec -T postgres pg_dump \
  -U d_concierge \
  -d d_concierge > backup/d_concierge_$(date +%Y%m%d).sql
```

Linuxアプリ直接配置版のPostgreSQLバックアップ例:

```bash
mkdir -p backup
pg_dump 'postgresql://d_concierge:<本番DBパスワード>@127.0.0.1:5432/d_concierge' \
  > backup/d_concierge_$(date +%Y%m%d).sql
```

Windowsアプリ直接配置版のPostgreSQLバックアップ例:

```powershell
New-Item -ItemType Directory -Force backup
pg_dump "postgresql://d_concierge:<本番DBパスワード>@127.0.0.1:5432/d_concierge" `
  | Out-File -Encoding utf8 "backup\d_concierge_$(Get-Date -Format yyyyMMdd).sql"
```

ファイル領域のバックアップ対象:

| 対象 | 内容 |
| --- | --- |
| `codex/.codex` | 生成用Codexホーム |
| `codex/.codex_validator` | 検証用Codexホーム |
| `codex/sessions` | 生成用Codex作業領域 |
| `codex/sessions_validator` | 検証用Codex作業領域 |
| `codex/saved_artifacts` | 保存済みCodex成果物 |
| `logs/trace` | 障害調査用トレースログ |
| `config.yaml` | 本番設定 |

Codexホームと `config.yaml` は認証情報または秘密値を含むため、バックアップ保存先のアクセス権限を制限する。Linuxでは `tar`、Windowsでは `Compress-Archive` を使用して、対象ディレクトリを任意のバックアップ保存先へ退避する。

### 11.3. 任意バックアップの自動化

Docker Compose版とLinuxアプリ直接配置版では、運用者判断で `cron` または `systemd timer` によりバックアップコマンドを定期実行する。Windowsアプリ直接配置版では、運用者判断でタスクスケジューラによりバックアップコマンドを定期実行する。

自動化する場合も、バックアップ結果の確認と復元確認は運用者が実施する。

## 12. 障害時対応

| 事象 | 初動 | 確認対象 | エスカレーション |
| --- | --- | --- | --- |
| 画面に接続できない | アプリケーションプロセスを確認し、必要に応じて再起動する | Dockerログ、systemdログ、タスク状態、ポート公開状態 | 運用者 |
| APIがエラーを返す | トレースログとアプリケーションログを確認する | `logs/trace`、アプリケーションログ、DB接続 | 運用者 |
| DB接続に失敗する | PostgreSQLの稼働状態と `config.yaml` のDB接続先を確認する | PostgreSQLログ、DBユーザ、DBパスワード、ネットワーク | 運用者 |
| Codex実行コンテナが失敗する | Docker、Codex実行用Dockerイメージ、Codexホーム、`codex_docker` 設定、AIサービス通信、トレースログを確認する | Docker、`codex-python-runner:latest`、`codex/.codex`、`codex/.codex_validator`、`config.yaml`、トレースログ | 運用者 |
| 容量不足が発生する | DB、Codex作業領域、保存済み成果物、トレースログの使用量を確認する | PostgreSQL、`codex/sessions`、`codex/sessions_validator`、`codex/saved_artifacts`、`logs/trace` | 運用者 |
| データ復旧が必要 | 任意バックアップの有無を確認し、復元対象を特定する | DBバックアップ、ファイルバックアップ、`config.yaml` | 運用者 |

再起動コマンド:

| パターン | コマンド |
| --- | --- |
| Docker Compose版 | `docker compose -f infra/compose.yml restart app` |
| Linuxアプリ直接配置版 | `sudo systemctl restart d-concierge` |
| Windowsアプリ直接配置版 | `Stop-ScheduledTask -TaskName "D-Concierge"`、`Start-ScheduledTask -TaskName "D-Concierge"` |
